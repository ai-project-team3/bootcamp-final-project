# 6. 통합 체크포인트

---

## 6-1. 일정

| 언제 | 무엇 |
|---|---|
| **9/22 (월)** | **킬러 피처 마감** — 멘토: *"월요일까지 피처 만들기. 이 앱을 왜 써야 하는지"* |
| **9/23 (화)** | **중간 발표.** 멘토: *"시연보다 덱이 중요"* |
| ~~10/5~~ | ~~추석 연휴~~ — **틀렸다(09-25).** 2026년 추석은 **9/24~9/26** 이고 이미 지났다. 2025년 달력을 옮긴 흔적이다 |
| 9/28 (월) · 9/30 (수) · 10/2 (목) | 멘토링 — 역할 확정 · 목소리 라인업 · 진행 방식 결정 |
| **10/4 (일)** | 🎯 **완성 — 실제 연결로 세 모드가 한 바퀴** (09-26 조장 목표 · §6-1-1) |
| **10/6 (화) 전** | ⚠️ **비공개 테스트 시작 마감** — 14일을 채워야 발표 전날(10/19)에 끝난다. 구글 사용자 인증 · vc4(시연 서랍 · PIN)가 여기에 걸린다 |
| **10/20 (화)** | **최종 발표 — 발표자 이종훈** (09-26 확정) |

**6주 전체에서 실질 구현 인력은 3명 남짓이다.** 조장이 문서·발표·법·섭외·병합에 상당 시간을 쓴다.

---

### 6-1-1. 로드맵 — 10/4 완성 → 10/20 발표 (09-26 · ⚠️ 임시, 9/28 확정)

**완성의 기준 (09-26 조장)** — **연결을 다 하고, 세 모드가 실제로 도는 것을 확인한다.**
실기기에서 아이가 **말하면** → 받아쓰기 → 판정이 **다음 질문을 정하고** → 마스코트가 **목소리로** 묻는다. 동화 · 일기 · 협업 셋 다.
지금은 셋 다 **대본**이다 — 버튼이 답을 꺼내고, 판정은 Kotlin `when` 문이고, 서버 라우터 셋은 `NotImplementedError`, 앱에는 `INTERNET` 권한이 없다.

**왜 1주에 되나 — 세 모드가 같은 입구를 지난다.**
답 받기는 `Director.awaitReply()` 한 곳, 판정은 `Director.judge()` 한 곳(`StoryBank.kt:926` — 일기 `DiaryScenes.kt:194` · 협업도 이걸 부른다), 목소리는 `say()`.
**이 셋을 진짜로 바꾸면 세 모드가 한꺼번에 연결된다.** 모드마다 남는 일은 자기 모드가 끝까지 도는지 확인하고 고치는 것이다.

#### 연결 분담 — 입구마다 한 사람 (임시)

| 누구 | 조각 | 어디 | 끝 |
|---|---|---|---|
| **이종훈** | **서버 전부** — `/stt`(whisper `large-v3` + 헛문장 거르기) · `/judge`(`gpt-6-luna` + 금칙어 필터) · `/tts`(TypeCast 중계 — 키는 서버에만) · **폰에서 닿는 주소** · 앱 네트워크 클라이언트 한 파일 · `INTERNET` 권한 | `backend/` · 새 클라이언트 파일 | **9/28 에 가짜 응답 서버 먼저** — 명세(`3_API_명세.md`)대로 고정 JSON 을 돌려준다. 팀원이 바로 붙일 수 있게 하고, 조장은 그 뒤에 진짜로 바꾼다 |
| **최민우** | **입력** — `awaitReply()` 를 마이크로: 말 끝 잡기(VAD — `DiagnosticsActivity.kt` 에 실측한 코드가 있다) → **폰에서 이름 가리기**(규칙 6) → `/stt` | `Director.kt` (공용 — 조장이 맡긴다) · `DiagnosticsActivity.kt` | 일기 모드가 말로 돈다 |
| **안치영** | **판정** — `judge()` 를 `/judge` 로. 판정이 돌려준 칸 · 다음 칸으로 흐름이 가게. 동화 모드 규칙 위반 정리(8턴 상한 → `story_ready` · 틀 빈칸 16개 → `extra` · `by`) | `StoryBank.kt` · `Scenes.kt` (본인) | 동화 모드가 판정대로 돈다 |
| **박진웅** | **출력 · 프롬프트** — 판정 · 생성 프롬프트 정본 하나로(9/30) · `say()` → TypeCast 목소리 재생 · 고정 대사 번들 | `eval/judge_prompt.md` · 새 재생 코드 | 협업 모드에서 마스코트가 목소리로 묻는다 |
| **모두** | **자기 모드가 실제 연결로 끝까지 도는지 확인** — 깨지는 곳은 자기 모드 안에서 고친다 | | 10/2 |

⚠️ **서버 · 공용 파일 변경은 조장 리뷰**(CODEOWNERS 도입 여부는 9/28). 입구 셋은 서로 맞물리므로 **9/28 에 주고받는 모양(명세 §3)을 먼저 맞춘다.**

#### 주차별

**1주차 9/28~10/4 — 연결**

| 날 | 무엇 |
|---|---|
| 9/28 (월) | 확정: 역할 · 목소리 라인업 · 연결 분담 · `dev` 브랜치 · **완성 기준** / 조장: 가짜 응답 서버 · 접속 주소 · 네트워크 클라이언트 |
| 9/29~9/30 | 조장: 서버를 진짜로 / 민우 · 치영 · 진웅: 입구 셋 / 진웅: 프롬프트 정본 / 일기 · 협업 흐름 확정(9/30) |
| 10/1~10/2 | **모드별 실제 한 바퀴 확인** / vc4 를 막는 것 — 시연 서랍(조장) · 부모 PIN(진웅) |
| 10/3~10/4 | 통합 버그 · 🎯 **완성 확인** · vc4 빌드 |

**1주차에서 뺀다 — 완성 기준 밖**: 세션 중 배경 생성(프리셋 그대로) · 화이트보드 실제 인식(시연 버튼 그대로) · 저장 · 판정 Jev 분할 · 책 낭독.

**⚠️ 10/5 — 비공개 테스트 시작** (구글 인증 · vc4 · 테스터 14명)

**2주차 10/5~10/11 — 채우기**: 세션 중 배경 생성 연결 · 화이트보드 실제 인식(ML Kit AutoDraw → Jev) · 이어 하기 · 책장 저장 · 목소리 라인업 굽기 · 오브젝트 배치 Jev · 대화 지연 실측

**3주차 10/12~10/19 — 다듬기 · 발표**: 테스터 피드백 반영 · 안정화 · 각자 발표용 한 장(10/16) · 덱 · 리허설

**10/20 최종 발표**

#### 위험

| | 무엇 | 왜 |
|---|---|---|
| 1 | **서버가 폰에서 닿아야 한다** | 받아쓰기 · 이미지가 조장 PC(GPU)에서 돈다. 팀원 에뮬레이터 · 실기기에서 닿는 주소가 9/28 에 필요하다. **비공개 테스트가 시작되면 테스터 폰이 언제든 닿아야 한다 — PC 가 꺼지면 앱이 멈춘다.**<br>**방향 (09-26 조장): 학원의 놀고 있는 PC 를 상시 서버로 쓴다.** 확인할 것 — ① **GPU**(`nvidia-smi`): 12GB 이상이면 조합 A 그대로, 작거나 없으면 그 PC 는 **API 서버만**(판정 · 목소리 · Jev 는 GPU 없이 돈다) 두고 받아쓰기는 폰 ML Kit · 배경은 프리셋으로 떨어진다(이미 정한 대체 경로) ② **상시 전원** — 밤 · 주말 전원, 절전 · 업데이트 재시작 끄기, 재부팅 뒤 서버 자동 시작 ③ **학원 허락**(외부 테스터가 닿는 서버다) ④ **키 보안** — 여럿이 쓰는 PC 면 전용 윈도우 계정에만 `.env`. 접속은 **고정 주소 터널**(학원 네트워크를 안 건드린다 · 앱에 박히는 주소라 바뀌면 안 된다) |
| 2 | 실제 말이 흐르기 **전에** 폰 이름 가리기 | 규칙 6. 입력 조각에 넣었다 |
| 3 | 구글 사용자 인증 | 우리가 앞당길 수 없다. 10/5 가 여기에 걸린다 |
| 4 | 1주에 연결 전부는 빠듯하다 | 밀리면 **동화 모드 하나를 먼저 완성**한다. 일기 · 협업은 같은 입구를 쓰므로 뒤따른다 |

**밀릴 때 빼는 순서**: 세 장 생성 → 판정 Jev 분할 → 서버 책장(폰 저장만) → 책 낭독 업체 분리(TypeCast 하나로)

---

## 6-2. 반드시 통과해야 하는 흐름

**이 14단계가 끝까지 끊기지 않으면 1차가 된 것이다.**

1. 앱을 켠다 → [같이 만들자]
2. 누구와 하는지 고른다
3. 주인공을 만든다 (프리셋 또는 그림)
4. 마스코트가 **기준 질문 ①** *"어디로 가 볼까?"*
5. 아이가 말한다 → **VAD가 구간을 자른다**
6. **STT → 폰에서 이름 가리기 → 금칙어 → 판정**
7. 판정이 `slot_1`·`value_1`을 돌려주고 **`next_slot`을 정한다**
8. 마스코트가 **받아주기 + 되돌려주기 + 다음 질문**
9. 마스코트가 **기준 질문 ②** *"왜 그럴까?"* → **2턴 답으로 초기 수준 확정**
10. **3턴째에 템플릿 확정** → 그 뒤 질문 결만 바뀐다
11. 중간에 **그림판 · 소리 녹음 · 미션**이 한 번씩
12. **`story_ready: true`** → 장면 6개 + 자막
13. **미리 만든 그림을 겹쳐 책이 되고, 그 자리에서 읽는다**
14. 부모 모드 → **오늘 아이가 한 말과 횟수**

**「친구 평가」는 책을 읽은 다음이다** (9/18 확정). 좋아/싫어로 다음에 또 볼지 정한다.

---

## 6-3. 지금 막고 있는 것

| 무엇 | 왜 막나 | 누가 |
|---|---|---|
| **API 키** | 판정·생성 모델을 못 고른다. Mistral만 발급됨 — **무료 티어면 분당 제한을 확인해야 한다** | 조장 |
| **아이 목소리 30발화** | **STT 모델 선정 전체.** 어른 모사로는 순위가 안 나온다(성인 7.8% vs 아동 120.8%) | 조장 |
| **AI-Hub** | ✅ **승인됨 (9/18 · 신청 즉시).** 남은 것은 다운로드와 30발화 고르기 | 조장 |
| **API 키** 추가 | 학원이 **쓴 만큼 한도 안에서 정산**해 준다. 선불이 필요해 아직 막혀 있다 | 조장 |


### 6-3-1. AI-Hub 한 줄

**지금은 수익화하지 않으므로 그냥 신청해 쓴다.** 부트캠프 프로젝트는 연구·학습 용도다.

**나중에 유료화한다면 그때 볼 것** — 이용정책 제7항이 *"상업적 이용은 수행기관과 별도 협의"*를 요구한다. 수행기관은 108번 NHN다이퀘스트, 540번 미디어젠. **그 시점의 숙제이지 지금 숙제가 아니다.**

---

## 6-4. 재는 숫자 — 합격선

**측정값이 이 선을 넘어야 확정한다. 기준 없는 측정은 아무것도 결정하지 못한다.**

| 항목 | 합격선 | 못 넘으면 |
|---|---|---|
| 판정 JSON 성공률 | **≥ 99%** | 스키마를 더 평평하게 → 상위 모델 |
| 아이 발화 핵심 키워드 보존율 | **≥ 90%** | STT 상향 또는 자체 파인튜닝 |
| 프리셋 매칭 정답률 (자모 편집거리 적용 후) | **≥ 95%** | 발음 규칙 정규화 추가 |
| **아이가 듣는 첫 소리** | **≤ 2초** | 파이프라인 기법 먼저 → 그래도 안 되면 모델 교체.<br>⚠️ **같은 3060의 공개 구현 실측이 2.98~4.10초다.** 우리 목표가 공격적이라는 뜻이고, **채우는 말(filler)이 그래서 필수다** |
| 판정 완료 | ≤ 4초 | 첫 소리가 먼저 나가므로 여유가 있다 |
| 캐릭터 동일성 (팀 5점) | **평균 ≥ 4** | 부품 합성 비중 확대 |
| **이야기 금지 표현** | **0건** | **타협 없음** |
| **한 권 총 AI 원가** | **≤ 200원** | 런타임 생성 장수를 줄인다 |

~~**한 권 원가에서 이미지가 90%를 차지한다.**~~
⚠️ **이 줄은 외부 이미지 API를 전제한 것이라 지금은 안 맞는다 (09-21 정정).**
그림을 **우리 GPU에서 만들기로 정했으므로** 권당 이미지 원가는 **전기값**이고,
지금 권당 값에서 실제로 세는 것은 **판정 21.6원**뿐이다(`eval/results.md`).
→ **「원가가 모자라서」를 이유로 무엇을 줄이는 판단은 다시 계산한 뒤에 한다.**

### ⚠️ 이 표에 없는 것 — `slot_1` · `next_slot`

**일부러 비어 있는 게 아니라 아직 못 정한 것이다.** 네 모델이 모두 0.43~0.66에 몰려 있어서
**선이 없으면 이 지표로는 아무도 못 떨어뜨린다.**

발표 자료에 **`slot_1` 정확도 88%**를 잠정으로 적었는데, **그 유도에 구멍이 둘 있다** —
「8쪽 중 7쪽」에서 뽑은 기준을 **턴별 정확도**에 붙였고(한 권은 약 19턴이다),
**숫자를 보고 나서 만들었다.** 그래서 **잠정이고, 모델을 떨어뜨리는 근거로 쓰지 않는다.**

확정하려면 **틀린 턴이 실제로 몇 쪽을 망치는지**를 먼저 세야 한다.

---

## 6-5. 되돌릴 신호

**아래가 보이면 규모를 줄이거나 방향을 되돌린다.**

| 신호 | 그러면 |
|---|---|
| 판정 JSON 성공률이 **전 모델에서** 99% 미만 | **모델이 아니라 스키마 문제다.** 필드를 줄인다 |
| **다중채움·필수해제 F1이 낮다** | **설계를 되돌린다** — 슬롯 하나씩 주는 방식으로 |
| 프리셋 매칭이 강도 1에서 95% 미만 | 선택지 UI를 병행한다 |
| 첫 소리가 4초를 넘는다 | 마스코트가 먼저 반응하고 뒤에서 판정한다 |
| 자체 faster-whisper가 3060에서 안 돈다 | Azure 오프라인 컨테이너로 |

---

## 6-6. 1차에 안 하는 것

로그인(**나중에 구글 로그인으로 얹는다**) · 결제 · 관리자 웹 · 월드 맵 · **세션 중 이미지 생성** · **자체 호스팅 LLM** · 수준 이월(매 세션 다시 판단한다)

---

## 6-7. 측정은 발표 자료이기도 하다

**재기 전 숫자를 남기지 않으면 최종 발표에 쓸 것이 없다.**

*"무엇이 느렸고 어떻게 고쳤다"*가 성립하려면 **부끄러운 베이스라인이 먼저 있어야 한다.** 측정할 때 **개선 전/후를 같은 줄에** 기록한다. `eval/results.md`가 그대로 슬라이드가 된다.

⛔ **베이스라인은 최적화를 붙이기 전에만 잴 수 있다.** 파이프라인이 처음 이어지는 날, **VAD·문장 쪼개기·병렬 생성을 얹기 전에** 잰다.
