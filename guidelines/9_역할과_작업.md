# 9. 역할과 작업 — 프로토타입 (2026-09-22)

**이 문서만 보고 각자 자기 몫을 시작할 수 있어야 한다.** 그게 안 되면 이 문서가 실패한 것이니 고쳐서 커밋한다.

---

## 9-1. 오늘의 기준

**세 모드가 각각 한 바퀴 도는 통합 버전.** 내일 이것을 캡처해 **사용자 흐름 순서로 어떤 기술이 쓰이는지** 발표한다(발표: 최민우).

**한 바퀴는 「대본으로」다.** 실제 음성 인식·LLM·음성 합성·그림 생성은 오늘 붙이지 않는다. 이유는 §9-2.

---

## 9-2. ⚠️ 시작 전에 알아야 하는 앱의 현재 상태

**모르고 시작하면 하루를 날린다. 세 줄만 기억하면 된다.**

**① 대화는 전부 대본이다.** 아이 발화는 `demo/StoryBank.kt`의 `Answer(...)` 문자열 약 220개다. 마이크 버튼은 녹음이 아니라 **대본 답을 꺼내는 스위치**다 — `demo/Director.kt:140` 주석에 그렇게 적혀 있다. 판정도 LLM이 아니라 Kotlin `when`문(`StoryBank.kt:751` `judge()`)이고, 그림 생성은 `pause(2400)`이다.

**② 앱은 네트워크를 못 쓴다.** `AndroidManifest.xml`에 `INTERNET` 권한이 없고 HTTP 클라이언트 의존성도 없다. **백엔드는 존재하지만 앱이 그 존재를 모른다.** 그리고 백엔드의 `/judge` · `/stt` · `/story`는 셋 다 `raise NotImplementedError`다.

**③ 진짜 음성 코드는 따로 있다.** `DiagnosticsActivity.kt`(312줄)에 Silero VAD · `SpeechRecognizer` · `TextToSpeech`가 **실제로 동작하고 실기기에서 측정까지 끝났다.** 앱 본체와 코드를 한 줄도 공유하지 않는다. **지우지 않는다** — 나중에 본체로 옮길 원본이다.

> **폴더를 새로 파지 않는다.** `android/`가 프로토타입 본체다. 화면 19개와 그림 122장이 이미 붙어 있고 죽은 선언은 4개뿐이다. 새 폴더는 되는 것을 다시 만드는 일이다.

---

## 9-3. 0단계 — 가름 작업 ✅ **끝났다 (09-22)**

> **안치영이 먼저 해서 `main`에 들어갔다** (`8504c67`). `StoryMode { STORY, DIARY, COOP }`가 생기고
> `DiaryScenes.kt`·`DiaryBank.kt`로 일기·협업이 분리됐고, 화면 검사 **52개가 통과**한다.
> **세 모드가 각각 한 바퀴 돈다 — 오늘의 기준은 채워졌다.**
>
> 아래는 무엇이 왜 필요했는지의 기록으로 남긴다. 그리고 **아직 안 된 것 하나** —
> `Scenes.kt`의 `runScene()`은 여전히 화면 하나로 갈라진다. 모드로 먼저 갈라지게 하는 건 남았다.

### (기록) 왜 이게 먼저였나

**맡는 사람: 이종훈**

지금 코드에 **모드라는 개념이 없다.** 세 사람이 이 상태로 동시에 모드를 붙이면 아래 네 곳에서 매 커밋 충돌한다.

| 파일 | 왜 터지나 |
|---|---|
| `demo/Model.kt:15` `enum class Scene` | 모드마다 화면을 더함 → 3자 충돌 |
| `demo/Scenes.kt:19` `runScene()` | 분기점이 하나뿐인 **1,420줄** 파일 |
| `demo/Model.kt:405` `DemoState` | 속성 70개 god object. 모두가 `resetStory()`를 같이 고침 |
| `ui/Screen.kt:202` `StageView` | `Stage` 20종을 모두가 확장 |

⚠️ **`Scenes.kt:45` `seedFor()`가 `Scene.entries.indexOf()`로 순서를 계산한다.** 화면을 맨 뒤가 아닌 곳에 끼우면 **남의 코드가 조용히 깨진다.**

**0단계에서 할 일**

1. `DemoState`에 `enum class AppMode { STORY, DIARY, COOP }` 추가
2. `Scenes.kt`를 `ScenesCommon.kt` / `ScenesStory.kt` / `ScenesDiary.kt` / `ScenesCoop.kt`로 쪼개고, `runScene()`이 **모드로 먼저 갈라지게** 한다
3. `seedFor()`의 순서 계산을 **명시적 목록**으로 바꾼다
4. 모드별 상태를 `DemoState`에서 분리해, 각자 자기 홀더만 고치게 한다
5. 이름 정리 세 가지 — §9-6

**끝나면 `main`에 올리고 팀에 알린다. 그 커밋 해시가 모두의 출발점이다.**

---

## 9-4. 역할 — **모드별로 한 사람씩** (09-22 저녁)

안치영이 프로토타입 골격을 혼자 세웠다. 그 위에서 **각자 자기 모드의 디테일을 붙인다.**

| 누구 | 모드 | 지금 상태 |
|---|---|---|
| **안치영** | **동화(일반) 모드** | 한 바퀴 돈다. 디테일을 붙인다 |
| **최민우** | **일기 모드** | 골격은 있다. 질문 단계 297줄이 여기에 있다 |
| **박진웅** | **부모 협업 모드 + 부모 모드 화면** | 골격은 있다. 부모 화면이 모드별로 안 갈라져 있다 |
| **이종훈** | **공통 · 병합 · 다음 2주** | — |

**진행 방식** — 각자 `feature/<이름>`에 올리고 **조장이 병합을 확인하며 진행한다.**

### ✅ 협업 분기를 파일로 뽑아냈다 (09-22)

일기와 협업이 `DiaryScenes.kt` 한 파일을 쓰고 있어서 민우·진웅이 같은 파일을 고치게 됐다.
협업 쪽만 **`demo/CoopScenes.kt`**로 옮겼다 — **테스트 52개 그대로 통과.**

| 옮긴 것 | 어디로 |
|---|---|
| `coopAsk()` · `coopReact()` | `CoopScenes.kt` (`coopAsk`은 `private`) |
| 첫 안내 4줄 | `coopIntro()` |
| 질문 분기 | `askOrCoopAsk()` — 일기 쪽은 **협업인지 모른다** |
| 끝날 때 표시·기록 | `coopFinishLog()` — 일기 모드에서는 아무 일도 안 한다 |
| 15분 안내 · 책 이름 · `pageAuthor()` | `coopTimeUpNote()` · `coopBookName()` · `pageAuthor()` |

**일기 쪽에 남은 `isCoop`은 한 줄**(`DiaryScenes.kt:124`)인데, 이건 *"협업이면 `coopIntro`,
아니면 일기 첫 대사"*를 고르는 갈고리라 남는 게 맞다.

> **반대로 뽑지 않았다.** 일기가 본체이고 협업이 그 위에 얹히는 관계다 —
> 협업을 빼도 일기는 돌지만, 일기를 빼면 협업은 안 돈다.

---

## 9-5. 역할별 — 소유 파일 · 다음 커밋 · 건드리면 안 되는 것

> **소유 파일**: 그 사람만 고친다. 남의 파일을 고쳐야 하면 **말하고 PR을 따로 연다.**

### 안치영 — 동화(일반) 모드

- **소유**: `demo/Scenes.kt` · `demo/StoryBank.kt` · 화면 검사(`ScreenShotTest` · `StoryTextTest`)
- **다음**: 시연 녹화에 쓸 경로를 **한 번 끊김 없이** 확인하고 걸리는 곳 수리
- **디테일 붙일 곳**: 이야기 틀 **7가지 중 2가지(B·F)가 아직 없다.** 넣을지 정하고 넣는다
- **건드리지 않는다**: `DiaryScenes.kt`(민우) · `CoopScenes.kt`·`ui/Parent.kt`(진웅) · `Model.kt` 공용부(조장)
- ⚠️ **틀 빈칸 14개를 지우거나 이름을 바꾸지 않는다** — §9-6
- ✅ **해결 (09-22)** — 동화 모드가 `EVENT` 장면에서 꼬리질문을 하고 답을 `slots["reaction"]`에
  넣는데 **어느 틀도 그 칸을 읽지 않았다.** 판정·수준 신호에만 쓰이고 아이가 한 말이 책에는 안 들어갔다.
  **넣기로 했다** — 차별점 1번(「아이 말이 그대로 책이 된다」)에 걸리는 자리라서다.
  - 다섯 틀 모두의 **사건 쪽(SHAKE)** 끝에 붙는다 — 질문이 바로 그 장면에서 나오므로
  - `plot`에는 **넣지 않았다.** `scenePlot`은 `t.plot`을 그대로 다시 묻기 때문에 넣으면 같은 질문을 두 번 한다
  - 질문 세 변형의 **값 모양이 달라** (결과 절 · 대사 · 사람 이름) 장면 4에서 `reactionLine()`으로
    문장을 지어 넣는다. 판정과 이벤트에는 아이가 한 말 그대로가 간다
  - 검사: `StoryTextTest.theChildsAnswerAtTheEventBecomesASentence` · `everyTemplatePutsThatSentenceInTheBook` ·
    `StoryFlowTest.theBookAtTheEndIsMadeOfWhatTheChildSaid`

### 최민우 — 일기 모드

- **소유**: `demo/DiaryScenes.kt`(협업 분리 후) · `demo/DiaryBank.kt`의 `DIARY_STEPS`·`diaryTemplate()`
- **다음**: 일기 한 바퀴를 직접 돌려 보고 **질문이 어색한 곳부터** 고친다. `DIARY_STEPS`(297줄)가 이 모드의 본체다
- **디테일 붙일 곳**: 문서가 *"일기 모드의 질문은 동화보다 어렵다 — 상상이 아니라 **기억을 꺼내야** 한다"*고 적어 뒀다. **못 떠올릴 때의 사다리**가 여기서 제일 중요하다
- ⚠️ **미결 하나** — 있었던 일을 **얼마나 부풀려도 되는가.** 문서가 *"사실을 재료로 동화를 만든다"*까지만 정했다. **오늘은 부풀리지 않는 쪽**으로 두고, 선을 어디 그을지 의견을 남긴다
- **새 슬롯을 만들지 않는다.** 일기도 **같은 12종**을 쓴다
- **건드리지 않는다**: `Scenes.kt`·`StoryBank.kt`(치영) · `CoopScenes.kt`·`Parent.kt`(진웅)
- 🕐 **발표자다.** `docs/발표_0923_기획안.md` 16장으로 **초안 → 대본 → 소리 내어 연습**이 먼저다. 시간을 먼저 떼어 두고 남는 걸로 모드를 한다

### 박진웅 — 부모 협업 모드 + 부모 모드 화면

- **소유**: `demo/CoopScenes.kt` · `ui/Parent.kt` · `ui/ParentBand.kt` · `ui/Shelf.kt`
- ✅ **요청한 홀더 넣었습니다 (09-22)** — `Model.kt`에 `parentQuestions`(미리 넣은 질문 목록) ·
  `parentQIndex`(소진 인덱스) · `hasParentQuestion` · `nextParentQuestion()`.
  **비어 있으면 옛 흐름으로 떨어지므로 넣기만 해도 안전합니다.** 이야기마다 비웁니다
- ⚠️ **소진 뒤에 무엇을 하는지는 진웅님이 정합니다** — AI 추천으로 넘어가나, 마스코트가 이어받나
- ⚠️ **설계가 바뀐 자리입니다.** `docs/부모협업모드_설계.md` §0은 *"AI가 귀띔하고 부모가 읽어 묻는다"*로
  적혀 있는데, **9/21 확인으로 「부모가 미리 넣어 두고 아이가 답한다」가 정본**입니다.
  코드(`CoopScenes.kt`)도 아직 옛 설계입니다. **문서 정정은 원저자(치영님)와 조율**합니다 —
  §9-7 규칙 2
- **다음**: **부모 화면을 모드별로 갈라 놓기.** 지금 6개 축이 동화 전용이라 일기 모드에서 값이 없다
- **디테일 붙일 곳**: 협업은 **부모가 질문을 미리 넣어 두는 모드**다. 그 **넣는 화면**이 제품의 절반이다 — LLM은 질문을 추천하는 정도로만 쓴다(지금은 추천도 대본)
- ⚠️ `Parent.kt`의 *"지난번"* 비교 막대는 **문자열 상수**다. 기록이 없다. **진짜인 척하지 않는다**
- **이 넷은 조장이 가져갔다 (09-22)** — 보호자 열람·정정·삭제 · 보존·삭제 수명주기 · AI 음성 고지 · 아이·부모·AI 분리 표시.
  문서는 조장이 쓰고(`docs/멘토조사_여섯.md`), **화면은 진웅님이 만들어야 한다** — 설계와 구현을 나눠 갖는다
- **건드리지 않는다**: `Scenes.kt`(치영) · `DiaryScenes.kt`의 일기 경로(민우)

### 이종훈 — 공통 · 병합 · 다음 2주

- **소유**: `demo/Model.kt` · `demo/Director.kt` · `ui/Screen.kt` · `MainActivity.kt` · `backend/` · `guidelines/` · `eval/`
- **첫 커밋**: §9-4의 **협업 분기 추출**. 이게 먼저다
- **그다음**: 이름 정리 세 가지(§9-6) → `runScene()`을 **모드로 먼저 갈라지게** → 병합
- **다음 2주**: `INTERNET` 권한과 네트워크 계층 → `/judge` 구현 → STT 이식
  ⚠️ **그 전에 벤더 콘솔을 본다** — 조직·프로젝트 id · 호출 엔드포인트 · 보존 설정(ZDR) · 계약 버전 · 계정 주인.
  지금은 대본이라 아이 데이터가 안 나가지만, **실제 연결이 그 「전」이다** (`docs/멘토조사_여섯.md` §2-2)
- **모드별 평가 케이스**를 `eval/`에 더한다 — 멘토 검토 5절. 기존 100건은 재사용하되 **모드별로 구별력을 갖는 것**을 더한다
- **멘토 조사 여섯이 전부 조장 몫이다** (09-22) — 문서 작업이 조장 몫이므로. 진행 상황은 `docs/조장_작업_체크리스트.md`

---

## 9-6. 이름 — **두 종류가 있고 섞으면 안 된다**

**이걸 모르고 "문서 이름으로 통일"하면 책 문장이 전부 기본값으로 떨어진다.**

| | **판정 슬롯 12종** | **틀 빈칸 14종** |
|---|---|---|
| 정본 | `guidelines/2_공통_데이터_모델.md` §1-1 | `StoryBank.kt`의 `TEMPLATES` |
| 누가 이름을 정하나 | **LLM이 닫힌 목록에서 고른다** | **앱이 미리 정해 두고 직접 묻는다** |
| 쓰는 곳 | 다음에 뭘 물을지 · 부모 리포트 집계 | **책 쪽 문장 렌더링** |
| 늘어나도 되나 | ❌ 늘면 앱이 못 알아본다 | ✅ 틀이 늘면 같이 는다 |

틀 빈칸은 틀마다 다르다 — E는 `stop saw resolve goal`, C는 `response helper resolve`, D는 `need role resolve`, A는 `try1 fail resolve reflect`, G는 `consequence lesson resolve reflect`.

책 문장이 이렇게 쓴다:

```kotlin
"${it.c}은 씩씩한 ${it.slot("role", "구조대원")}이 되기로 했어요"
```

**`role`이 비면 "구조대원"이 들어간다.** 즉 빈칸을 없애면 **아이가 한 말 대신 우리가 써둔 말이 책에 들어간다.** 차별점 1번을 정면으로 어긴다.

### ✅ 이름 정리 (09-22 완료)

**처음에 셋이 어긋났다고 적었는데 재보니 둘은 이미 맞았다.** 치영의 병합이 `reaction`·`companion`·`extra`를
이미 넣어 놨다. **밖으로 나가는 이름**만 세 곳 고쳤다.

| 고친 것 | 왜 |
|---|---|
| `follow` → **`reaction`** | 동화 모드 슬롯 키·이벤트·질문 은행 6곳. 12종 이름은 `reaction`이다 |
| 이벤트 `partner` → **`adult`** | 앱 변수는 `partnerHelp`여도 **나가는 이름은 문서를 따른다** |
| 이벤트 `sight` → **`extra` (`of: sight`)** | `sight`는 **틀 빈칸**이라 12종 밖이다. 규칙 1대로 `extra`로 나간다 |

**변수 이름은 안 바꿨다.** `s.friend`·`s.partnerHelp`는 31곳·5곳인데 **슬롯 계약과 무관**하다 —
계약은 `slots[...]` 키와 이벤트 payload로만 나간다. 큰 이름 바꾸기를 지금 할 이유가 없다.

⚠️ **왜 이게 중요한가** — 나중에 진짜 LLM을 붙이면 모델은 문서를 보고 `reaction`을 돌려주는데
앱이 `follow`를 찾는다. **그 칸은 조용히 비고 오류도 안 난다.** 그리고 `sight`가 `slot_filled`로
새어 나가면 **부모 리포트가 12종과 빈칸을 섞어 집계해 숫자가 거짓이 된다.**

**검사 52개 통과.**

---

## 9-7. 충돌을 줄이는 규칙 넷

1. **`feature/<본인이름>`에서 작업하고 `main`에 직접 푸시하지 않는다.** PR을 열고 조장이 병합한다
2. **남의 소유 파일을 고쳐야 하면 먼저 말하고 PR을 따로 연다.** 내 모드 PR에 끼워 넣지 않는다
3. **AI에게 맡길 때 diff를 작게 끊는다.** 한 번에 큰 diff를 만들면 충돌이 그만큼 커진다. **바이브코딩은 충돌을 줄여 주지 않는다 — 키운다**
4. **하루에 두 번 `main`을 받아 온다** (점심 · 저녁). 마지막에 몰아서 병합하지 않는다

---

## 9-8. 오늘 안 하는 것

**A 기준을 지키기 위해 의도적으로 뺀다.**

- **네트워크 · 실제 LLM 판정 · 실제 STT · 실제 TTS · 실제 그림 생성**
  이유: 앱에 `INTERNET` 권한조차 없고 백엔드 세 엔드포인트가 전부 미구현이다. 한 모드만 진짜로 만들어도 **약 10시간**이고, 오늘 잰 값으로 **아이가 기다리는 시간이 6.26초**(받아쓰기 2.54 + 판정 2.40 + 첫 소리 1.32)라 **느린 게 영상에 그대로 찍힌다**
- **화자 구분** — 설계가 바뀌어 빠졌다. 아이가 혼자 쓰고, 부모는 질문을 미리 넣는다
- **책장에서 다시 읽기** (`Scenes.kt:1375`에 이미 "아직 없다"고 적혀 있다)
- **저장** — `SharedPreferences` · `DataStore` · `Room` 아무것도 없다. 세션이 끝나면 다 사라진다. **오늘은 그대로 둔다**
- **죽은 코드 정리** — 안 쓰이는 선언 4개와 그림 2.2MB뿐이다. 5분짜리라 아무 때나 한다

---

## 9-9. 다 됐는지 보는 법

**네 줄이 전부 참이면 오늘 끝이다.**

- [x] 모드 선택 화면에서 **세 모드로 각각 들어간다** — `Scenes.kt`의 모드 선택
- [x] 세 모드가 각각 **끊기지 않는다** — 화면 검사 52개 통과
- [x] `main`에 병합돼 있다 — `8504c67`
- [ ] **부모 모드가 세 모드 중 어느 것을 하고 와도 안 터진다** ← **남았다 (진웅)**
- [ ] 다른 사람 기기에서 빌드된다 ← 각자 한 번 확인
