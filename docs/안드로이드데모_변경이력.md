# 안드로이드 데모 — 변경 이력

> 짝 문서: `안드로이드데모_설계.md`(구조) · `안드로이드데모_시나리오.md`(대본) · `안드로이드데모_트러블슈팅.md`(막혔던 것)
> **새 버전은 맨 위에 추가한다.** 과거 항목은 고치지 않는다 — 왜 그때 그렇게 했는지가 남아야 하므로.
> 기능뿐 아니라 **그림 · 문구 · 배치 변경도** 적는다.

| 버전 | 날짜 · 시각 | 한 줄 요약 |
| --- | --- | --- |
| [v0.15](#v015--선물-화면에서-앱이-멎던-것) | **9/22** | 일기·협업 모드에서 **그림을 안 그린 날 선물 화면에서 앱이 멎던 것** — 선물 개수로 끝을 판단하던 것을 `Gifts.done` 으로 고침 |
| [v0.14](#v014--아이가-한-말이-책에-안-들어가던-칸-하나) | **9/22** | 동화 모드 시연 경로를 **한 바퀴 돌려 보는 검사** · 「사건」 장면의 답(`reaction`)을 **책 문장으로** 넣기 · 안드로이드 스튜디오 폴더를 깃과 맞춤 |
| [v0.13](#v013--고친-것-열일곱-가지) | **9/22** | 안경 그림에서 **렌즈 칠과 접힌 다리 제거** · 눈 살색을 그림에서 뽑기 · 소리말 · 자세를 **자막에서** 읽기 · 진행 막대를 질문 수로 · 상호작용 안내는 **책에서 4.5초** · 협업 자막 번갈아 · 도화지 넓히기 · `bg_today` 생성 · **도감 멈춤(toPixelMap)** · 검사 6건 |
| [v0.12](#v012--고친-것-열여섯-가지) | **9/21** | 주인공 **삭제 · 성별 · 하의** · 안경+눈 버그 · 그림판(12색 · 지우개 · 굵기) · **장소에 맞는 동행 친구** · 일기 미션을 말한 일에서 · 책을 기승전결로 · 협업 띠에서 버튼 빼고 마스코트 · 설계 · 시나리오 문서 갱신 |
| [v0.11](#v011--일기-모드를-열-걸음으로--부모-협업-모드) | **9/21** | 일기 질문을 **열 걸음(8~12턴)** 으로 늘리고 흐름을 고침 · 장소에 맞는 배경 6장과 미션 소품(ComfyUI 19장) · **부모 협업 모드** |
| [v0.10](#v010--일기-모드--오늘-있었던-일로-짓기) | **9/21** | **일기 모드** — 오늘 있었던 일을 묻고 그 사실로 6~8쪽 동화책 · 기승전결 네 자리 · 사다리 · 출처 `by` 3종 |
| [v0.9.1](#v091--팀-이름-스플래시-clap) | **9/17** 17:55 ~ 18:30 | 앱을 켜면 팀 이름 **CLAP** 스플래시 |
| [v0.9](#v09--함께-하는-사람--질문-은행--수준별-템플릿--배경-속-것) | **9/17** 15:10 ~ 17:15 | 함께 하는 사람을 녹음으로 묻기 · 질문 은행(변형 46개 · 더미 답 258개) · 수준별 템플릿 E/C/D/A/G · 6~8쪽 · 배경 그림 속 것이 반응 |
| [v0.8](#v08--화면-전체-쓰기--소크라틱-질문--오브젝트--대화로-만든-미션--책장--부모-설정) | **9/17** 오후 ~ 15:05 | 화면 이름 · 맨 위 진행 막대 · 떠 있는 말풍선 · 소크라틱 질문 · 배경 오브젝트 · 대화로 만든 미션 · 책장 · 부모 설정 |
| [v0.7](#v07--구현대본-반영--열린-질문--친구-평가--어른-칸) | **9/16** 16:12 ~ 17:25 | 구현대본 반영 — 모든 질문 열린 질문 · [직접 그리기] 상시 · 친구 평가 · 어른 칸 · 6축 리포트 · 눈밭 배경 |
| [v0.6](#v06--용량-정리와-문서화) | **9/16** 14:50 ~ 15:50 | 그림 90MB → 18MB · 팀 공유용 문서 4종 · 배포 zip |
| [v0.5](#v05--아이콘을-전부-펠트-그림으로--장소별-미션) | **9/16** 14:30 ~ 14:50 | 이모지 전부 교체(42장) · 미션이 장소에 맞게 갈아 끼워짐 · 커스텀 질문 수정 |
| [v0.4](#v04--동화책-품질--커스텀-ui-통일--답-무작위) | **9/16** 11:50 ~ 14:30 | 그림책다운 6쪽 · 주인공 27장 + 눈 스티커 · 질문당 답 5개 무작위 |
| [v0.3](#v03--comfyui-그림--글꼴--앱-아이콘) | **9/16** 10:30 ~ 11:50 | 펠트 3D 그림 24장 · Jua 글꼴 · 마스코트 앱 아이콘 |
| [v0.2](#v02--문서-교차검증--마이크-온오프--아이가-고른-것이-책이-된다) | **9/16** 10:00 ~ 10:30 | 🎤 온/오프 · 장소 3곳 · 아이 선택이 책 자막까지 |
| [v0.1](#v01--안드로이드-첫-이식) | **9/16** 09:34 ~ 10:00 | HTML 데모 → Kotlin + Compose 이식 (장면 15개) |
| [v0.0](#v00--html-데모-교차검증) | **9/15** 저녁 ~ **9/16** 오전 | HTML 데모를 문서와 대조해 고침 |

### 하루 타임라인 (9/16)

굵은 시각은 **파일 수정 시각으로 확인한 지점**이고, 나머지는 그 사이 구간이다.

```
 09:34  프로젝트 생성 (사용자가 Android Studio에서)   ┐
 09:36  Compose 얹고 첫 빌드 성공                     │ v0.1
 09:50  에뮬레이터에서 장면 1~14 한 바퀴              ┘
 10:00  문서 교차검증 · 🎤 온/오프 · 장소 3곳          ─ v0.2
 10:33  Jua 글꼴 (jua.ttf)                            ┐
 11:02  첫 그림 생성 스크립트 (gen_assets.py)          │ v0.3
 11:21  그림 연결 구조 (Assets.kt)                     ┘
 11:52  눈 위치 검출 (find_eyes.py)                    ┐
 12:04  주인공 눈 위치 표 (HeroEyes.kt)                 │ v0.4
 12:19  주인공 27장 생성 (gen_heroes.py)               ┘
 14:32  책 전면 재작성 (Book.kt)                       ┐ v0.5
 14:36  아이콘 42장 생성 (gen_icons.py)                ┘
 14:50  그림 96장 리사이즈 · clean 빌드                ┐ v0.6
 15:50  문서 4종 · 배포 zip                            ┘
 16:12  조장이 구현대본.md를 냄                         ┐
 16:26  체크리스트 · 뒤에서 설명 갱신 (Behind.kt)       │
 16:51  눈밭 배경 · 어른 칸 소품 생성 (gen_extra.py)    │ v0.7
 17:07  장면 · 모델 마지막 수정 (Scenes.kt · Model.kt)  │
 17:25  에뮬레이터 2회 통과 · 문서 갱신 · zip 20.2MB    ┘
```

### 하루 타임라인 (9/17)

굵은 시각은 파일 수정 시각으로 확인한 지점이다.

```
 오후   요청 받음 · 그림 26장 생성 시작 (에뮬레이터 끄고)   ┐
 14:20  책장 배경까지 생성 끝 (gen_extra.py)              │
 14:24  새 그림 리사이즈 (512 · 384px)                    │ v0.8
 14:32  화면 공통 요소 (Chrome.kt)                        │
 14:48  부모 모드 고정 머리 (Parent.kt)                   │
 14:54  clean 빌드 · 3회 통과 → 문서 · zip               ┘
 15:10  v0.9 요청 · 함께 하는 사람 그림 6장 생성 시작        ┐
 15:28  그림 생성 끝 (에뮬레이터 끈 채로)                  │
 15:43  모델 · 질문 은행 · 템플릿 (Model.kt · StoryBank.kt)│
 16:17  배경 속 것 층 (Hotspots.kt)                        │
 16:30  에뮬레이터 1회(E 6쪽) · 2회(A 8쪽) 통과            │ v0.9
 16:40  "누구랑 하는지 녹음으로" 추가 요청 → 카드 없앰      │
 17:00  3회(C 7쪽) · 4회(눈 · 할머니) · 글자 검사 테스트    │
 17:15  문서 · zip                                        ┘
 17:55  스플래시 요청 · 에뮬레이터 다시 켬                ┐
 18:01  스플래시 화면 (Splash.kt) · 창 배경색             │
 18:06  기기에서 켜기 → 스플래시 → 첫 화면 확인           │ v0.9.1
 18:15  아래 문구 · 밑줄 빼기 · 이름 CLLAP → CLAP          │
 18:30  에뮬레이터 다시 부팅 · 확인 · 문서 · zip          ┘
```

---

## v0.15 — 선물 화면에서 앱이 멎던 것

**9/22** · 진웅님이 **실기기에서** 찾아 보고해 주었다. 책을 다 읽고 선물 화면에 오면 앱이 멎는다.
일기 모드와 협업 모드에서, **그림을 한 획도 안 그린 날**에만 난다. 동화 모드는 영향이 없다.

### 왜 멎었나 — 파일 두 개가 서로 다른 것을 보고 있었다

```
Scenes.kt  sceneEnd()   그림을 안 그린 날은 무지개 크레용을 건너뛴다 → Stage.Gifts(1) 에 머문다
                        (안 한 일에는 선물을 주지 않는다 — 조사3 §1-3)
Screen.kt  GiftsView    [📚 책장에 꽂기] 를 stage.shown >= 2 일 때만 그린다
                        (선물이 항상 둘이던 때의 조건)
Scenes.kt  sceneEnd()   awaitValue("shelf") — 아이가 누를 버튼이 없으니 영원히 기다린다
```

**선물 개수로 「끝났는가」를 판단한 것**이 어긋남의 뿌리다. 선물이 하나인 날이 생기면서 조건이 틀려졌다.

### 고친 방법 — 끝났다는 것을 직접 말한다

| 파일 | 고친 것 |
| --- | --- |
| `demo/Model.kt` | `data class Gifts(val shown: Int, val done: Boolean = false)` |
| `demo/Scenes.kt` | 크레용을 주는 갈래는 `Gifts(2, done = true)`, 건너뛰는 갈래는 `Gifts(1, done = true)` |
| `ui/Screen.kt` | `if (stage.shown >= 2)` → `if (stage.done)` |

`shown` 은 **불이 켜진 선물 수**, `done` 은 **더 나올 선물이 없다**는 뜻으로 나눴다.

### 왜 우리 검사가 못 잡았나

시연 서랍에는 `buttons(DemoBtn("📚 책장에 꽂기"))` 로 버튼이 **뜬다.** 서랍 버튼으로 미는 흐름 검사
(`StoryFlowTest` · `DiaryFlowTest`)는 그래서 멀쩡히 통과했다. 멎는 것은 **아이 화면**이다.

진웅님이 쓴 `CoopFlowTest.theShelfButtonAppearsOnTheChildScreenAfterTheGifts` 는 서랍이 아니라
**화면 조건(`Stage.Gifts`)** 을 본다. 고친 뒤 `@Ignore` 를 떼고 켰다.
단정도 `shown >= 2` 에서 `done` 으로 바꿨다 — 안 그린 날은 선물이 하나뿐이라 개수로는 여전히 빨갛다.

> 되짚어 확인했다 — 고친 줄을 잠깐 되돌리면 그 검사가 **빨갛게 된다.** 결함을 실제로 잡는 검사다.

**교훈: 시연 서랍으로 미는 검사는 아이 화면에 버튼이 있는지를 보지 않는다.**
아이가 눌러야 넘어가는 자리는 서랍 말고 **화면 상태**를 봐야 한다.

### 검사

85개 모두 통과 (전 81 + main 합치며 들어온 3 + 켠 것 1).

---

## v0.14 — 아이가 한 말이 책에 안 들어가던 칸 하나

**9/22** · 동화(일반) 모드를 맡게 되어 먼저 **시연 녹화에 쓸 경로를 끝까지 돌려 보았다.**

### 1. 시연 경로를 한 바퀴 돌려 보는 검사 (`StoryFlowTest`)

일기 모드·협업 모드는 한 바퀴를 돌아 보는 검사가 있었는데 **동화 모드만 없었다.**
있던 것은 시작 부분만 봤다 — 「함께할 사람」 화면까지, 「장소」 화면까지.
시연 경로가 중간에 끊기면 **녹화 당일에야** 알게 된다.

에뮬레이터 없이 감독을 그대로 띄우고 대본 버튼으로 시작 화면 → 책 → 시작 화면까지 밀어 본다.
**결과: 경로는 끊기지 않는다.**

> ⚠️ 처음 돌렸을 때 「확인」·「만드는 중」이 경로에서 빠진 것으로 나왔는데 **앱이 아니라 검사가 못 본 것**이었다.
> 한 걸음 밀 때마다 한 번씩만 장면을 봤기 때문에 스스로 넘어가는 장면을 놓쳤다. 따로 지켜보는 코루틴으로 바꿨다.

### 2. 「사건」 장면의 답이 책에 안 들어가던 것

장면 4에서 꼬리질문을 하고(*「그다음엔 어떻게 됐을 것 같아?」*) 답을 `slots["reaction"]`에 넣는데,
**다섯 틀 어느 쪽도 그 칸을 읽지 않았다.** 판정·수준 신호에는 쓰였으니 버려진 건 아니지만,
**아이가 대답한 말이 책에는 안 나왔다.** 「아이 말이 그대로 책이 된다」가 첫 번째 차별점이라 넣었다.

다섯 틀 모두의 **사건 쪽(2쪽)** 끝에 붙는다. 예:

```
전  그런데 로켓이 덜컹덜컹 흔들렸어요. 창밖을 보니 외계인 뭉치가 손을 흔들고 있었어요.
후  그런데 로켓이 덜컹덜컹 흔들렸어요. 창밖을 보니 외계인 뭉치가 손을 흔들고 있었어요. 그러자 멈췄어요.
```

**한 가지 걸린 것** — 질문 세 변형이 저장하는 값의 **모양이 서로 다르다.**

| 변형 | 묻는 것 | 저장되는 값 | 책 문장 |
| --- | --- | --- | --- |
| `follow_next` | 그다음엔 어떻게 됐을까 | 결과 절 — "쿵 떨어졌어" | 그러자 쿵 떨어졌어요. |
| `follow_say` | 뭐라고 말했을까 | 대사 — "으악!" | 지호는 "으악!" 하고 외쳤어요. |
| `follow_who` | 누가 제일 놀랐을까 | 사람 이름 — "민준이" | 민준이가 제일 깜짝 놀랐어요. |

그대로 이으면 책에 *"민준이."* 같은 토막이 남는다. 그래서 **장면 4에서** 어느 변형이었는지를
보고 문장을 지어 넣는다(`reactionLine()`). 틀은 3턴째에야 정해져서 **책을 그릴 때는
어느 변형이었는지를 알 수 없기** 때문이다. 바로 위 `problem` 칸도 같은 식으로 장면에서 문장을 만든다.

> ⚠️ `plot`에는 **넣지 않았다.** `scenePlot`은 `t.plot`을 그대로 다시 묻기 때문에
> 넣었다면 **같은 질문을 두 번** 하게 된다. 판정과 이벤트에는 아이가 한 말 그대로가 간다.

### 3. 안드로이드 스튜디오 폴더를 깃과 맞춤

검사를 두 군데서 돌리다 **스튜디오가 여는 프로젝트가 깃보다 뒤처져 있는 것**을 발견했다
(검사 62개 vs 79개). 팀원들이 `main`에 올린 것이 안 들어와 있었다.
**시연 녹화는 스튜디오에서 하므로** 그쪽이 깃과 같아야 한다. 소스 16개와 빌드 파일 3개를 맞췄다.

> android-vad 는 JitPack 에만 있어 `settings.gradle.kts` 저장소 · `libs.versions.toml` ·
> `app/build.gradle.kts` 의 ABI 추림이 **한 묶음**이다. 셋 다 가져와야 빌드가 된다.

### 검사

81개 모두 통과 (전 79 + 이번 2).

| 검사 | 보는 것 |
| --- | --- |
| `StoryFlowTest.theDemoPathRunsOneFullLapWithoutGettingStuck` | 시작 → 책 → 시작으로 돌아오는가 · 어디서 멈췄는지 남긴다 |
| `StoryFlowTest.theBookAtTheEndIsMadeOfWhatTheChildSaid` | 필수 칸 여섯 · 빈 자막 없음 · 6~8쪽 · **꼬리질문 답이 책에 실렸는가** |
| `StoryFlowTest.theProgressBarIsFullWhenTheStoryIsDone` | 진행 막대가 끝까지 차는가 |
| `StoryTextTest.theChildsAnswerAtTheEventBecomesASentence` | 세 변형이 각각 문장이 되는가 |
| `StoryTextTest.everyTemplatePutsThatSentenceInTheBook` | 다섯 틀 모두가 그 문장을 싣는가 · 안 말했을 때 틈이 안 생기는가 |

---

## v0.13 — 고친 것 열일곱 가지

**9/22** · 에뮬레이터로 돌려 본 뒤 받은 지적을 네 차례에 걸쳐 고쳤다. v0.12와 같은 성격이다 —
새 기능이 아니라 **이미 있는 것이 어긋나 있던 것**을 맞췄다. 마지막에 `main` 을 합쳐 협업 모드 분리와
최민우님의 정직한 폴백을 받아 왔다.

### 주인공 커스텀

| 무엇 | 왜 | 어디 |
| --- | --- | --- |
| 안경 그림에서 **렌즈 칠과 접힌 다리를 지웠다** (불투명 65% → 14%) | `gl_round.png` 는 생성된 그림이라 **렌즈 안쪽이 불투명한 흰색**이고 접힌 안경다리가 렌즈 뒤에 같이 그려져 있었다. 화면 기준 66%가 그 둘이다. 얼굴에 올리니 **흰 원 두 개**가 생기고 베이지색 다리가 머리카락 위로 삐져나왔다. 테두리(짙은 갈색)만 남기고 밝은 부분을 투명하게 깎았다 | `tools/fix_glasses.py` · 원본은 `tools/orig/` |
| 눈 살색을 **그림에서 뽑아 쓴다** | 반달 · 별 눈은 원래 눈을 살색으로 덮고 그 위에 그리는데, 그 값이 코드에 박혀 있었다(`255,220,194`). 실제 그림은 `247,213,184` 라 **눈 자리만 색이 뜨고 펠트 질감이 끊겼다.** 이제 두 눈 사이(콧대) 한 점을 읽는다. 가장자리도 방사형으로 풀어 붙인 자국을 없앴다 | `HeroImage.kt` `skinNear` |
| **눈을 먼저, 안경을 나중에** 그린다 | 순서가 거꾸로라 살색 패치가 안경테를 덮었다. 실제로도 안경은 눈 위에 걸친다 | `HeroImage.kt` |
| ⚠️ **도감에서 화면이 멎던 것** | 위의 살색 뽑기를 `remember` 없이 넣었다. `toPixelMap()` 은 640×640 = **41만 화소를 통째로 읽는다.** 도감은 주인공 카드를 **네 장 동시에** 그리는 화면이라, 다시 그릴 때마다 164만 화소를 읽었다. 그림 이름이 같으면 색도 같으므로 `remember(name)` 으로 감쌌다. 트러블슈팅 **6-15** | `HeroImage.kt` |

### 책 — 그림이 글을 따라간다

| 무엇 | 왜 |
| --- | --- |
| 소리말을 **쪽 종류가 아니라 자막에서** 읽는다 | `PageKind.SHAKE` 쪽이면 내용과 무관하게 **늘 "쿵!"** 이 뜨고 화면이 지진처럼 흔들렸다. 동화 모드에서는 탈것을 흔드는 쪽이라 맞았지만, 일기·협업의 같은 자리는 「오늘 있었던 일」 쪽이라 **그림 그린 날에도 "쿵!"** 이 올라갔다. 이제 자막에서 고른다 — `와르르! · 쿵! · 촤악! · 훌쩍… · 다다닥! · 티격태격! · 까르르! · 덜컹! · 빙글빙글! · 깜짝!`, 맞는 게 없으면 **아무것도 안 띄운다** (`effectFrom`) |
| **웃거나 우는 쪽은 안 흔든다** | 무너지거나 부딪히거나 덜컹거린 쪽에서만 흔든다. "까르르 웃었어요" 에 화면이 흔들리면 아이가 무서워한다 (`quakeFrom`) |
| **자세도 자막에서** 읽는다 | "그네를 탔어요" 라고 적혀 있는데 주인공이 가만히 서 있으면 글과 그림이 따로 논다. 그림을 새로 만들지 않고 몸짓으로 보여 준다 — 그네는 머리 위를 축으로 크게, 미끄럼틀은 비스듬히, 뛰기는 통통 (`motionFrom`) |
| **어긋난 쪽에서는 움직이지 않는다** | "달리다가 넘어졌어요" 가 '달리'에 걸려 통통 뛰었다. 소리말은 「쿵!」인데 그림은 신나게 뛰고 있었다. 넘어짐 · 무너짐 · 울음이 들어간 쪽은 자세를 끈다 |
| 지은이에서 **「· 함께 {어른}」을 뺐다** | 어른이 지은 자리가 하나도 없는 날에도 늘 붙어서, 아이가 혼자 지은 책에 어른 이름이 올라갔다. 이 책의 지은이는 아이다 |

### 책 — 문장이 이어진다

| 무엇 | 왜 |
| --- | --- |
| 잇는 말을 **두 문장의 결**로 고른다 | 하루 전체에 사고가 하나라도 있으면 무조건 "그런데"(반전)를 붙였다. 그래서 *"조심조심했지만 손이 흔들리고 말았어요. **그런데** 그림책을 함께 읽었어요."* 처럼 읽다가 멈칫했다. 이제 사고→사고는 「게다가」, 평온→사고는 「그런데」, **사고→평온은 「그래도」**, 평온→평온은 「그러고 나서」다 (`linkFor`) |
| 맺음 쪽 **차례를 바로잡았다** | *"그렇게 하루가 저물었어요. 집에 와서 손을 씻었어요."* — 하루가 끝난 **뒤에** 집에 왔다. 집에 와서 한 일이 먼저고 맺는 문장이 뒤다 |
| 결말 칸이 비면 **하루가 두 번 저물지 않게** | 비었을 때 쓰던 문장이 맺음 쪽과 겹쳤다. 비면 이 쪽은 건네주는 장면 하나로만 둔다 |
| 아무 일 없던 날에 **「왜 그랬는지는 아직 아무도 몰라요」** 가 붙던 것 | 없던 사고가 있었던 것처럼 읽혔다. 즐거웠던 날에는 "오늘은 그게 제일 좋았어요" 로 |
| 미션 문장이 **따로 놀던 것** | 이야기 옆에 별개 문장으로 붙어 한 쪽에 아이 이름이 두 번 나왔다("그래서 지호는 다시 쌓았어요. 지호는 모래를 치웠어요"). 아이 말이 있으면 **주어를 빼고 이어 붙인다** (`m1Caption(withSubject)` · `m2Clause()`) |
| 일기 미션에서 **가방이 사라졌다** | 일기 모드엔 탈것이 없어서 대신 **가방**을 띄워 놓고 거기에 흙을 묻혔다. 아이가 가방 이야기를 한 적이 없다. 이제 **놀던 자리(바닥)** 를 치운다 |
| 아무도 없던 날에 **「그 친구」를 지어내지 않는다** | 받는 사람이 없으면 마스코트가 받는데, 앞에 한 번도 안 나온 인물이라 뜬금없었다. "오늘 이야기를 들어준 마스코트에게" 로 자리를 만들어 준다 (`hasCompanion` · `giveTargetName`) |
| 선물이 **결말과 겹치던 것** | *"반창고를 붙이고 다시 놀았어요. 그리고 민서에게 반창고를 붙여 주었어요."* — 결말에 이미 나온 물건은 고르지 않는다 |
| 미션 2의 **친구 크기** | 동화 모드의 공룡·외계인과 같은 크기로 그려 주인공보다 두 배 컸다. 일기·협업에서 받는 쪽은 또래 아이다 |

### 화면

| 무엇 | 왜 |
| --- | --- |
| 진행 막대가 **물은 질문 수**로 찬다 | 필수 칸(일기 4 · 동화 6)만 세어, 일기에서 질문 열둘을 다 답해도 막대가 3분의 1에서 멈췄다. 세 모드 다 같은 증상이었다. 고치고 나서도 **마지막 질문에서 안 차는** 문제가 남았다 — 꼬리질문은 `turn` 을 안 올리는데 총량에는 세었고, 동화 모드의 `resolve` 는 `slots` 가 아니라 `solution` 으로 들어가 **절대 안 차는 칸**이 하나 있었다. 지금은 **지나온 걸음 수**로 세고, 이야기가 끝나면(`endReason`) 끝까지 찬다 |
| 막대가 **뒤로 물러나던 것** | 동화 모드는 템플릿이 3턴째에 정해진다. 그전 분모가 6이었다가 정해지는 순간 9~10으로 늘어 비율이 **줄어들었다.** 정해지기 전에는 최대치로 잡아 두고, 분모가 줄어도 뒤로 가지 않게 막았다 |
| 상호작용 원을 **책에서 4.5초만** 보여 준다 | 아이가 말한 자리가 책을 덮을 때까지 **내내** 반짝여 그림 읽기를 방해했다. 처음엔 "여기 눌러 볼 수 있어" 를 알려 주는 좋은 신호지만 계속 반짝이면 눈이 그리로만 간다 |
| 안내가 **질문 화면부터 뜨던 것** | `HotspotLayer` 에 소개 여부를 안 넘기는 호출부가 하나 남아 기본값 `true` 가 됐다. 역할을 `introducing`(전부 반짝 = 안내)과 `glowMentioned`(아이가 말한 것만)로 갈랐다. 기본값도 `false` 로 바꿨다 |
| 안내가 **한 번도 안 보이던 것** | 시간을 책이 열릴 때부터 쟀는데 **표지(0쪽)에는 배경 자리가 없다.** 아이가 표지를 4.5초 넘게 보면 원이 뜨기도 전에 끝났다. 이제 `HotspotLayer` 가 **원을 실제로 그리는 순간부터** 센다 |
| 일기·협업에서 안내가 **안 뜨던 것** | `diaryGlow` 가 아이 말과 겹치는 낱말만 담아 비어 있는 날이 많았다. 소개는 모드와 상관없이 **누를 수 있는 자리를 전부** 보여 준다 |
| 안내가 **두 번째 이야기부터 안 뜨던 것** | `hotspotIntroShown` 을 이야기 초기화 목록에 안 넣었다 |
| 그리기 **도화지를 넓혔다** | 크레용을 46dp로 키웠다가 되돌렸다 — 팔레트가 커진 만큼 그릴 자리가 줄어 손해였다. 되돌린 폭과 가장자리 여백을 도화지에 돌렸다 |

### 부모 협업 모드

| 무엇 | 왜 |
| --- | --- |
| 자막 둘이 **번갈아** 뜬다 | 말풍선과 부모 띠가 **둘 다 화면 맨 아래**에 놓여 겹쳤다. 그걸 피하려고 띠가 떠 있는 동안 말풍선을 통째로 숨겨 두었는데, 그 바람에 **아이가 답한 뒤 마스코트 반응이 사라졌다.** 협업에서 부모에게 넘긴 것은 「다음 질문」 하나뿐이고 받아주기·되돌려주기는 마스코트 몫이므로 반응은 보여야 한다. 이제 `say()` 가 띠를 비우고 `askSay()` 가 말풍선을 비운다 |
| 사다리 칸 수를 **`parentAsk` 로 센다** | 위 변경으로 `parentCard` 가 수시로 비워지는데 그걸로 세고 있어 **사다리를 내려가도 칸이 안 세어졌다.** 화면 표시와 기록을 갈랐다 |
| 🎤 · ➡️ 를 **늘 오른쪽 아래**에 | 띠를 피해 172dp 위로 올라가 화면 중간에 떠 있었다. 버튼은 자리를 지키고 **띠가 168dp를 비켜 준다**(`END_RESERVE`) |

### 그림 자산

| 무엇 | 왜 |
| --- | --- |
| **`bg_today` 를 만들었다** (ComfyUI) | 아이가 말한 곳이 등록된 열 곳에 안 걸리면(*"이모네 농장 갔어"*) 이 이름으로 떨어지는데 **그 파일이 없었다.** 배경이 통째로 비어 보였다. 공원 같은 실제 장소로 떨어뜨리면 안 된다 — 농장에 갔다는 아이에게 놀이터를 보여 주면 앱이 하루를 지어내는 셈이다(일기 §3-2). **어디라고 말하지 않는** 노을 들판으로 뽑았다 | 프롬프트는 `tools/gen_assets.py` 의 배경 목록 |

### 검사 — 여섯 개를 더했다 (57 → 62건, `main` 병합 포함)

| 검사 | 지키는 것 |
| --- | --- |
| `diaryBookReadsAsOneStory` | 하루 다섯 가지의 책을 **글로 떨궈**(`build/diary_samples.txt`) 이름 두 번 · 없는 친구 · 잇는 말 겹침을 막는다. 동화 모드에는 같은 검사가 있었는데 일기·협업 책은 **아무도 읽어 본 적이 없었다** |
| `theSoundWordComesFromTheCaptionNotThePageKind` | 소리말 규칙 + **동화 모드가 잃은 게 없는지**(템플릿 다섯의 흔들림 쪽 전부) |
| `theMotionComesFromTheCaptionAndStopsWhenSomethingWentWrong` | 자세 규칙 · 어긋난 쪽에서 안 움직이는지 |
| `everyBackgroundTheCodeAsksForActuallyExists` | 코드가 찾는 배경이 **파일로 있는지.** 없으면 조용히 색 배경으로 떨어져 눈으로 봐도 놓친다 |
| `coopBottomArea` | 말풍선 · 띠 · 버튼이 겹치지 않는 배치 |
| `aSecondStoryStillGetsPastTheBestiary` · `aSecondStoryModeRunStillGetsPastTheBestiary` | 책을 만들고 **끝까지 넘겨** 시작 화면으로 돌아온 뒤, 두 번째 이야기의 도감을 지나가는지 (일기 · 동화 모드) |

### `main` 병합으로 받아 온 것

| 무엇 | 누구 |
| --- | --- |
| 협업 모드를 **`CoopScenes.kt` 로 분리** — 일기와 협업의 주인을 나눴다. `DiaryScenes.kt` 에는 갈고리 셋만 남는다(`coopIntro` · `askOrCoopAsk` · `coopFinishLog`) | 박진웅 |
| 마스코트가 메운 결말에 **「마침내」를 붙이지 않는다** — 아이가 지은 결말이 아닌데 붙이면 책이 아이 말인 척한다 | 최민우 |
| 일기 걸음 수를 `DIARY_STEPS.size` 로 세기 · 블록 놀이에 물감 미션이 나오던 것 | 최민우 |

⚠️ 병합할 때 `m2Caption()` 에서 부딪혔다. 쓰는 곳이 없어 지웠는데 최민우님 결말 코드가 쓰고 있었다.
`m2Clause()` 로 바꿔 합쳤고, **두 분 의도를 다 살렸다** — 「마침내」를 안 붙이는 것과 맺음 쪽과 안 겹치는 것 둘 다.

---

## v0.12 — 고친 것 열여섯 가지

**9/21** · 에뮬레이터로 한 바퀴 돌려 본 뒤 받은 지적 열여섯 가지를 고쳤다. 새 기능이 아니라 **이미 있는 것이 어긋나 있던 것**을 맞춘 작업이다.

### 주인공 만들기

| 무엇 | 왜 | 어디 |
| --- | --- | --- |
| 도감이 다 차면 **지울 수 있다** (🗑) | 네 칸이 다 차면 새 주인공을 만들 수 없었다. 마지막 한 명은 못 지운다 — 도감이 비면 이야기를 시작할 수 없다 | `Scenes.kt` `sceneBestiary` · `Screen.kt` |
| **주인공 그림을 부품으로 나눴다** (81장 → 27장 + 얹는 안경) | 토글 하나를 누르면 **캐릭터가 통째로 다른 아이로 바뀌었다** — 머리를 길게 하면 얼굴이 여자아이가 되고, 옷 색을 바꾸면 하의가 치마가 되고, 안경을 바꾸면 몸 비율이 달라졌다. 81장이 각각 독립 생성된 그림이라 서로 아무 관계가 없었기 때문이다. 얼굴·자세를 못 박아 몸 9장을 뽑고 거기서 **img2img로 머리만** 바꿔 27장을 만들었다. 안경은 그림에서 빼고 얹는다 — **안경+눈 어긋남도 이걸로 원인부터 사라졌다.** 트러블슈팅 **5-11** |
| 「바지」가 치마와 구분이 안 됐다 | 다리 구분 없는 통짜로 나왔다 → "두 다리가 갈라지고 발이 보이게" 를 프롬프트에 못 박았다 |
| ~~**성별**(남 · 여) 고르기~~ → **뺐다** | 고르면 하의 · 머리 기본값이 따라오게 만들었다가, 같은 날 요청으로 **다시 뺐다.** 성별을 먼저 묻고 옷을 정해 주는 순서가 아니라 아이가 바지든 치마든 그냥 고르면 되는 순서가 낫다. 하의 선택은 그대로 남는다 | `Art.kt` `HeroAttr` |
| **하의**(바지 · 치마 · 반바지) 고르기 | 안경 아래 줄에 넣었다. 치마 · 반바지 54장을 `gen_heroes_bottom.py` 로 만들었다. 긴바지는 이름을 그대로 두어 기존 27장을 그대로 쓴다 | `Model.kt` `heroImageName` |
| **안경 + 반달/별 눈**이 이상하게 나오던 것 | `find_eyes.py` 가 안경 쓴 그림에서 **안경테**를 눈으로 잡았다(`r` 이 렌즈 크기). 자리는 맞았는데 그 위에 **살색** 패치를 칠해서 렌즈 한가운데에 살구색 동그라미가 생겼다. 안경이면 패치를 **렌즈 색**으로 칠하고 그 안에 표정을 그린다 | `HeroImage.kt` |

### 그림 그리기

| 무엇 | 왜 |
| --- | --- |
| 질문 옆 **[직접 그리기] 버튼을 뺐다** | 까닭 · 이름 · 끝처럼 **말하는** 질문에까지 붙어 있어 거의 모든 질문에 떠 있었다. 그리기는 **무언가를 만드는 자리**에만 둔다 — 일기 모드는 `diaryDrawStep()`(오늘 만난 사람 그리기)이 그 자리이고, 부모 리포트의 '만들기' 축도 그 걸음이 채운다 |
| 크레용 **4색 → 12색** | 살구 · 갈색까지 넣어 사람도 그릴 수 있게 |
| **지우개** 추가 | [지우기]가 전부 지워서 한 획만 고칠 수가 없었다. 지우개는 **닿은 자리에서 획을 끊는다** — 긴 낙서 한 획이 통째로 사라지지 않게. 전부 지우기는 [모두 지우기]로 남겼다 |
| 그린 것이 **뚱뚱하게** 나오던 것 | 붓이 화면 픽셀 고정(16f)이었고 책에서는 화면 크기로 다시 계산해서, 작게 그린 그림일수록 선이 부풀었다. 굵기를 **그림판 폭 기준**(`PEN_W`)으로 두고 `Stroke` 에 실어 책까지 같은 값으로 그린다 |

### 이야기가 아이 말과 맞나

| 무엇 | 왜 |
| --- | --- |
| **어디를 가든 공룡이 나오던 것** | 장면 7이 공룡 셋만 알고 있어서, 우주에 가도 바닷속에 가도 "공룡도 데려갈래!" 하고 공룡이 로켓에 탔다. 장소마다 **그곳에 있을 법한 친구 셋**을 둔다 — 우주(외계인 친구 · 로봇 · 아기 별) · 바닷속(돌고래 · 해마 · 불가사리) · 공룡 나라(그대로) · 눈 오는 데(눈사람 · 북극곰 · 펭귄). 아이가 먼저 하는 말도 소리도 같이 바뀐다 (`gen_buddies.py` 9장) |
| 일기 미션이 **뜬금없던 것** | 장소만 보고 골라서, 장소를 못 알아들으면 "진흙이 가방에 묻었어요" 가 나왔다. 이제 ① **아이가 말한 일**(물감 · 블록 · 모래 · 나뭇잎 · 물 · 간식) → ② 장소 → ③ 둘 다 없으면 **무엇이 묻었다고 지어내지 않고** 하루 먼지를 턴다 |
| 책이 **중구난방**이던 것 | 일기 책의 승(承) 쪽이 일어난 일을 먼저 쓰고 하던 일을 뒤에 붙여서 거꾸로 읽혔다("블록이 무너졌어요. 지호는 블록을 높이 쌓아 올렸어요"). **하던 일 → 그런데 → 일어난 일** 순서로 놓고 잇는 말을 넣는다. 잇는 말이 겹쳐 붙지 않게 막아 두었다 (`joinWith`) |
| 전(轉)에 **아이가 한 일**이 없던 것 | "왜"와 "뭐라고 했어" 둘뿐이라 어긋난 일과 끝 사이가 비어 있었다. `try` 걸음("그래서 ○○는 어떻게 했어?")을 더해 열한 걸음이 되었다 |
| 책이 **적힌 대로 움직이지 않던 것** | "로켓을 타고 떠났어요" 인데 주인공이 탈것 옆에 서 있고, "손을 흔들고 있었어요" 인데 가만히 있었다. 쪽 자막의 낱말을 보고 **탈것 위에 올리거나 몸을 흔든다** |
| 일기 장소 그림 **4곳 추가** | 키즈카페 · 병원 · 수영장 · 동물원. 여섯 곳뿐이라 자주 말하는 곳이 색 배경으로 떨어졌다 (`gen_diary_more.py`) |

### 부모 협업 모드

| 무엇 | 왜 |
| --- | --- |
| 띠에서 **[다르게 물어볼래] · [내가 답할래] 를 뺐다** | 버튼을 고르는 것이 일이 되어 아이와 이야기하는 흐름이 끊겼다. 질문을 바꾸는 것도 마지막에 채우는 것도 **동화 모드와 똑같이** 마스코트가 한다. "어른이 읽고 물어봐 주세요" 안내는 그대로 둔다 |
| `coop:adult` 가 화면에 나오던 것 | 그 버튼이 보내던 값이 칸으로 새어 들어갔다. 버튼과 함께 사라졌고, **칸에 `coop:` · `adult:` 값이 들어가지 않는지 검사로 막아 두었다** |
| 띠 왼쪽에 **마스코트** | 글을 못 읽는 아이에게 "마스코트가 물어본다"로 보이게 |
| 어른이 읽어 준 질문을 기록 | [내가 답할래]가 사라지면서 `adultLine` 이 빌 뻔했다. 어른이 읽고 물어본 **마지막 질문**을 부모 리포트의 "어른이 한 말"로 남긴다 |

### 배경과 그림

| 무엇 | 왜 |
| --- | --- |
| 일기 장소 **4곳 추가** (키즈카페 · 병원 · 수영장 · 동물원) | 여섯 곳뿐이라 자주 말하는 곳이 색 배경으로 떨어졌다. 병원은 첫 장이 놀이방처럼 나와서 접수대 · 키 재는 자 · 구급상자를 넣어 다시 뽑았다 (`gen_diary_more.py`) |
| 일기 배경에 **누를 자리** | 동화 책은 배경 속 것이 통 튀는데 일기 책만 가만히 있었다. 열 곳에 손으로 좌표를 잡아 넣고, **아이가 말한 낱말과 겹치는 것만** 반짝이게 했다 (`diaryGlow`) — 앱이 없는 것을 만들어 내는 게 아니라 아이 말에 있던 것만 켠다 |
| 같이 갈 친구 **9장** | 우주 · 바닷속 · 눈 오는 데 각 3장 (`gen_buddies.py`). 공룡 3장은 그대로 쓴다 |
| `tools/shrink_assets.py` | 만든 그림을 넣기 전에 줄인다 — 오려낸 것 640px · 배경 1344×768. v0.10부터 그림이 크게 늘어 손으로 줄이기 어려워졌다 |

### 그 밖에 맞춘 것

- **고쳐 만들기(장면 9)의 더미 답**도 장소를 따라간다 — 우주에 가 놓고 "다른 공룡 할래"가 나오지 않는다
- **부모 리포트의 "오늘 n번 다르게 물어보셨어요"** — 첫 질문까지 세고 있었다. 사다리로 **바꿔 물은 것만** 센다
- **어른이 읽어 준 질문을 기록** — [내가 답할래]가 사라지면서 `adultLine` 이 빌 뻔했다. 어른이 읽고 물어본 마지막 질문을 남긴다

### 에뮬레이터로 확인하며 또 고친 것

화면으로만 보이는 것들이라 돌려 보기 전에는 몰랐다.

| 무엇 | 왜 |
| --- | --- |
| 골라서 만들기의 **[이걸로 할래]가 말풍선에 가려졌다** | 줄이 넷에서 여섯으로 늘면서 버튼이 아래 말풍선 자리까지 내려왔다. 토글 오른쪽 빈 자리로 옮겼다. 안내 문구도 "성별, 머리, 옷, 눈, 안경, 아래옷을 골라 봐!"로 |
| 안경 + 반달/별 눈에 **원래 눈이 남았다** | `find_eyes.py` 를 새 그림 54장에 다시 돌리지 않아 눈 위치 표가 28개에 멈춰 있었다(→ 75개). 게다가 새 그림에서는 안경테가 아니라 **눈동자**를 잡아서(r 이 작다) 패치가 눈을 못 덮었다. **무엇을 잡았는지는 반지름을 보면 안다** — 크면 안경테, 작으면 눈동자. 눈동자는 세로로 긴 타원이라 타원으로 덮는다 |
| 장면 7을 **건너뛰면 여전히 공룡**이 따라갔다 | 반응 예산 초과나 시연 서랍으로 건너뛰면 `dinoKey` 가 기본값 `horn` 그대로였다. 장소를 고르는 순간 기본 친구를 그 장소 것으로 맞추고, 건너뛴 경우에도 한 번 더 확인한다 |
| 더미 답 **"혼자서 빛나요 거!"** | 책 자막용 종결형(`look`)에 "~ 거!"를 붙였다. 관형형(`said`)을 따로 적는다 — **한국어 어미는 규칙으로 바꾸면 깨진다** |
| 시연 서랍으로 장면을 건너뛰면 **부모 띠가 남았다** | 책에서는 자막까지 가렸다. 일기 장면을 벗어나면 띠를 비운다 |
| 부모 리포트가 **없어진 버튼을 누르라고 안내**했다 | "[다르게 물어볼래]를 눌러 보세요" — 그 버튼은 뺐다. 이제 사다리는 아이가 말하지 않을 때 마스코트가 내리므로, 누가 바꿨는지 단정하지 않는 문장으로 고쳤다 |
| 협업 리포트가 **"어른이 지은 자리 0"** 이라고 했다 | [내가 답할래]를 빼면서 그 수가 언제나 0이 되어 "어른이 아무것도 안 했다"로 읽혔다. 실제로는 어른이 **모든 질문을 읽어 주었다** → "어른이 읽어 준 질문 n번"으로 |
| 「뒤에서」 설명이 **없어진 버튼과 사라진 60초 안전망**을 말했다 | 시연 때 보여 주는 화면이라 그대로 두면 안 된다. 지워진 함수의 고아 주석도 치웠다 |
| 탈것에 **올라탄 게 아니라 위에 떠 있었다** | 발이 몸통에 겹치도록 내렸다 |
| 장면 이름이 **"장면 7 · 공룡도 데려갈래"** | 장소마다 친구가 다른데 이름에 공룡이 박혀 있었다 → "친구도 데려갈래" · "친구 소리" |
| `run_emulator.ps1` 이 **파싱 오류**로 안 돌았다 | PowerShell 5.1이 BOM 없는 UTF-8을 ANSI로 읽어 한글 주석이 깨졌다 → UTF-8 **BOM 포함**으로 저장 (트러블슈팅 6-13) |
| **바닷속에 "외계인 뿌뿌"** 가 서 있었다 | 장면 7의 공룡과 **똑같은 뿌리**다 — 장면 4를 건너뛰면 `newcomerKind` 가 기본값 "외계인" 그대로였다. 장소를 고를 때 그 장소의 새 친구로 같이 맞춘다. ⚠️ 문장을 만들기 **전에** 맞춰야 한다("외계인이 거북이를 흔듦"이 남는다) |
| 배경 누를 자리 두 곳이 **빈 곳을 가리켰다** | 어린이집 「작은 의자」가 탁자 위에, 동물원 「기린」이 목 옆에 있었다. `tools/check_hotspots.py` 로 **그림 위에 원을 그려** 열네 곳을 한 번에 대조했다 — 에뮬레이터보다 정확하고 켜지 않아도 된다 |

### 검사

`./gradlew test` **52개 모두 통과.** 이번에 더한 것:
- 책이 기승전결 순서로 읽히고 잇는 말이 겹치지 않는지 (`theBookReadsInOrderWithLinkingWords`)
- 협업 띠에 버튼이 없고, 말없이 두면 사다리가 저절로 내려가고, 칸에 `coop:` 값이 새지 않는지
- 미션 소품이 ① 말한 일 ② 장소 ③ 먼지 순서로 골라지는지
- 배경에서 **아이가 말한 것만** 반짝이는지 (`theBackgroundLightsUpOnlyWhatTheChildSaid`)
- 같이 갈 친구가 **아이가 고른 장소의 것**인지 · 관형형이 종결형으로 새지 않는지 (`theBuddyAlwaysBelongsToThePlaceTheChildChose`)
- 자막의 낱말대로 타거나 손을 흔드는지, 그리고 **"기차가 흔들렸어요"를 인사로 읽지 않는지** (`thePagePoseFollowsWhatTheCaptionSays`)
- 장면을 건너뛰어도 그 장소의 것이 나오는지 (`skippingASceneStillLeavesThePlaceConsistent`)

- 「골라서 만들기」가 **성별을 묻지 않는지** · 몸 그림이 27가지인지 · **안경과 눈을 바꿔도 몸 그림이 안 바뀌는지** (`theHeroBuilderAsksForLooksNotForGender`)
- **토글 하나를 바꾸면 그 하나만 바뀌는지** — 다섯 줄을 한 장에 그려 대조 (`changingOneToggleChangesOnlyThatThing`)

새 도구: `tools/check_hotspots.py` — 배경 누를 자리를 **그림 위에 그려** 대조한다 (에뮬레이터 불필요)

**화면 검사를 붙였다** — Robolectric + Roborazzi로 Compose 화면을 **JVM에서 PNG로 그려** 기준과 대조한다
(`ScreenShotTest` · 기준 그림은 `app/screens/`). 에뮬레이터·하이퍼바이저를 쓰지 않아 기계가 얼지 않고 27초면 끝난다.
지금 잡아 둔 화면 8장: 골라서 만들기 · 안경+별 눈 · 부모 띠 · 그림판 · 도감(네 칸) · **미션 문지르기 · 미션 끌어다 놓기 · 토글 다섯 줄 대조**.

**손가락 터치와 애니메이션도 에뮬레이터 없이 확인한다** — `performTouchInput` 으로 드래그를 흉내내고
`mainClock` 으로 시간을 민다. 미션 1(문질러서 모래 털기) · 미션 2(별을 친구에게 건네기) · 쪽 애니메이션이
**실제로 동작하는 것**을 그림으로 확인했다. 트러블슈팅 **6-15**.

> 이 기기에서 에뮬레이터가 하루에 **네 번** 기계를 얼렸다(트러블슈팅 6-12). 그래서 **화면에만 있던 값을 밖으로 꺼내
> 검사 대상으로 만드는** 쪽으로 옮겼다 — 「골라서 만들기」 줄 목록(`HERO_ROWS`), 책의 자세 판정(`ridingFrom` · `wavingFrom`),
> 배경 좌표(`check_hotspots.py`). 화면을 띄우지 않고도 확인되고, 실제로 더 정확했다.

### 문서

`안드로이드데모_설계.md` 가 9/18에서 멈춰 있어 일기 · 협업 모드가 아예 없었다. §3 구조 · §4-8(모드 셋) ·
§4-9(책이 아이 말과 어긋나지 않게) · §6 그림 스크립트 · §7 화면 규칙 · §8 제한을 채웠다.
`안드로이드데모_시나리오.md` 도 "우주에 온 트리케라톱스"를 **정상 동작으로 적어 두고 있었다** — 장면 7 · 8 · 9를 고치고
**부록 I(일기 · 부모 협업 모드)** 를 새로 썼다.


## v0.11 — 일기 모드를 열 걸음으로 · 부모 협업 모드

**9/21**

**왜**: 사용자 요청 다섯 가지 — ①*"누구랑 같이 만들지 물어보는 기능을 빼봐"* ②*"질문 수를 늘려줘(스무고개 느낌으로).
질문 수가 적으면 모이는 데이터가 적어서 동화책의 퀄리티가 떨어질 거 같아"* ③*"질문의 흐름이 약간 이상하거든"*
④*"미션이나 이미지 요소들을 알맞게" · "배경도 사용자가 말하는 거에 맞게 · ComfyUI 사용해서"*
⑤*"부모협업모드_설계.md 파일을 보고 이 기능도 만들어줘"*

### ① 함께할 사람을 묻지 않는다 (일기 · 협업)

동화 모드는 그대로 묻는다. 일기 모드는 재료가 **아이의 실제 하루**라 "오늘 누구와 있었는지"를
**이야기 안에서** 묻는 게 자연스럽고(둘째 걸음), 협업 모드는 옆에 있는 사람이 곧 질문하는 사람이라 고를 이유가 없다.

### ② 네 질문 → 열 걸음 (8~12턴)

| | 칸 | 묻는 것 | 필수 |
|---|---|---|---|
| 기 | `place` | 오늘 어디 갔었어 | ✅ |
| 기 | `companion` | 거기 누구랑 있었어 | |
| 승 | `problem` | 무슨 일이 있었어 | ✅ |
| 승 | `extra`(detail) | 그때 뭐 하고 있었어 | |
| 승 | `reaction` | 기분이 어땠어 | |
| 전 | `cause` | 왜 그랬을까 (**S1**) | ✅ |
| 전 | `extra`(said) | 그 사람이 뭐라고 했어 | |
| 결 | `solution` | 그래서 어떻게 됐어 (S2) | ✅ |
| 결 | `extra`(after) | 다 끝나고 뭐 했어 | |
| 맺음 | `extra`(keep) | 내일 또 하고 싶은 거 | |

**새 칸 이름은 하나도 만들지 않았다.** 꼬리질문 답은 슬롯 12종의 `extra`(*"위 어디에도 안 맞는 것 · 원문 그대로"*)에 쌓인다.
판정 스키마 16필드도, 최민우에게 넘긴 평가셋 100개도 그대로다.
책은 여전히 6~8쪽이지만 **쪽마다 아이 말이 한 문장씩 더 들어가고 마스코트가 메울 자리가 줄었다** — 질문을 늘린 이유가 이것이다.

### ③ 흐름 — 앞 답에서 다음 질문이 나온다

전에는 *"왜 그랬을까?"* 가 앞뒤 없이 나왔다. 블록을 쌓았다고만 말한 아이에게는 물을 데가 없는 질문이다.

- 장소를 들으면 다음 질문이 *"{장소}에서 무슨 일이 있었어?"*
- 사람을 들으면 *"{누구}가 뭐라고 했어?"* 가 열리고, **혼자 논 날에는 그 걸음을 건너뛴다**
- 일이 어긋난 날에만 *"왜 그렇게 됐을까?"*, 그냥 즐거웠으면 *"그게 왜 제일 좋았어?"*
- 이미 마음을 말했으면 기분을 또 묻지 않는다

### ④ 배경 · 미션이 하루에 맞는다 (ComfyUI 19장)

`tools/gen_diary.py` — `gen_assets.py` 와 **같은 모델 · 같은 스타일 문자열**(krea2 turbo · 양모 펠트)을 써서 한 세트로 보이게 했다.

| 무엇 | 몇 장 |
|---|---|
| 일상 장소 배경 (놀이터 · 어린이집 · 할머니 집 · 공원 · 마트 · 집) · 늦은 오후 빛 | 6 |
| 미션 1 흔적 (모래 · 물감) · 미션 2 건넬 것 (블록 · 그림책 · 반창고) | 5 |
| 등장인물 프리셋 (선생님 · 친구 둘) | 3 |
| 협업 모드 배지 (같이 만들기 · 다르게 물어볼래 · 내가 답할래) | 3 |
| 번갈아 짓기 표시 (아이 · 어른) | 2 |

- **배경**은 아이가 말한 곳으로 고른다. 맞는 게 없으면 **엉뚱한 곳을 보여 주지 않고** 해 질 녘 색으로 둔다
  (실제 앱은 세션 중에 만든다 — CLAUDE.md 규칙 8)
- **미션 1**은 그 장소에서 묻은 것을 턴다 — 놀이터면 모래, 어린이집이면 물감, 공원이면 나뭇잎
- **미션 2**에 건넬 것은 아이가 말한 것에서 — 블록 · 그림책 · 반창고 · 딸기 · 별

### ⑤ 부모 협업 모드 (`부모협업모드_설계.md`)

**부모에게 소재를 받는 모드가 아니라 질문하는 사람을 바꾸는 모드다** (§0).

| | |
|---|---|
| 넘기는 것 | 마스코트 한 턴 세 조각 중 **③ 다음 질문만** (§2-2) |
| 안 넘기는 것 | 받아주기 · **되돌려주기** · 낭독. 고쳐 말해 주되 지적하지 않는 것이 부모가 가장 못하는 일이다 |
| 새 화면 | **부모 띠 하나** — 질문 카드가 소리 없이 뜨고 부모가 자기 말로 묻는다 (§3) |
| 기다리는 시간 | 5초 · 8초를 쓰지 않는다. 부모가 옆에 있으므로 AI가 끼어들지 않는다. 60초 안전망만 (§3-1) |
| [다르게 물어볼래] | 사다리를 **부모가 손으로** 내린다 (§5) |
| [내가 답할래] | 그 자리가 `adult` 칸이 된다 (§4-2) · 책에 작은 표시가 남는다 (§6) |
| 출처 | **`by: parent` 를 만들지 않는다.** 부모 발화는 `payload.speaker: adult`, 부모가 지은 자리는 `by: mascot` + 누가 지었는지만 따로 (§4-1) |
| 부모 리포트 | 점수가 아니라 **다음에 써 볼 질문 한 개** (§7 · 형태만 먼저 넣음) |

부모 자리가 절반을 넘으면 띠에 귀띔이 뜬다 — 그건 부모가 짓고 아이가 들은 것이지 협업이 아니다 (§6).

### 손댄 파일

새로: `demo/DiaryScenes.kt`(일기 · 협업 장면과 질문 엔진) · `ui/ParentBand.kt`(부모 띠) · `tools/gen_diary.py` · 그림 19장
수정: `demo/DiaryBank.kt`(열 걸음 · 책) · `demo/Model.kt`(모드 3종 · 동행 · 배경 매핑 · 번갈아 짓기) ·
`demo/Scenes.kt`(갈림길 · 함께할 사람 건너뛰기) · `demo/Missions.kt`(장소별 소품) · `demo/Behind.kt` ·
`ui/Screen.kt` · `ui/Book.kt`(누가 지었는지 표시) · `ui/Parent.kt` · `MainActivity.kt`

### 확인

- `assembleDebug` 통과 · 새 경고 없음 · 단위 검사 **37개 전부 통과** (일기 · 협업 24 · 기존 13)
- 기존 `StoryTextTest`(템플릿 5 × 함께할 사람 6 × 장소 4 전수 자막 검사)가 그대로 통과 → **동화 모드 회귀 없음**
- 검사가 실제 버그를 하나 잡았다 → 아래

### 검사와 에뮬레이터가 잡은 것 셋

1. **`seedFor` 가 협업 모드를 일기 모드로 덮어썼다.** 장면을 열 때 `if (scene == DIARY) mode = DIARY` 로 두었더니
   [같이 만들기]로 들어가도 부모 띠가 뜨지 않고 조용히 일기 모드로 떨어졌다.
   `!s.isDiary` 조건을 붙여 고쳤고, 그 자리를 검사로 막아 두었다(`coopRunsToTheBookAndMarksWhoWroteWhat`).
2. **부모 띠와 🎤 · ➡️ · [직접 그리기] 가 겹쳤다.** 띠는 화면 폭을 다 쓰는데 컨트롤은 오른쪽 아래에 떠 있었다.
   띠가 있을 때 컨트롤을 띠 위로 올려 고쳤다 (에뮬레이터 화면으로 확인).
3. **책에 아이가 말한 사람이 안 섰다.** 동행을 새로 받게 되면서 `showFriend` 가 "아이가 그렸을 때"만 보고 있었다.
   그린 것 → 말한 사람 순으로 세우도록 고쳤다 (둘 다 없으면 아무도 세우지 않는다 · 일기 §3-2).

### 에뮬레이터 확인 (Pixel_3a)

시작 화면 세 갈래 → [같이 만들기] → **함께할 사람을 안 묻고 바로 도감** → 부모 띠에 질문 카드(소리 없음) →
[다르게 물어볼래]로 사다리 한 칸 → 아이가 "어린이집"이라 답하자 **배경이 어린이집 교실로** →
꼬리질문 "거기 누구랑 있었어?" → "동생이랑" 답하자 **동생이 프리셋 그림으로 무대에** →
일이 어긋난 날이라 "왜 그렇게 됐을까?" → **8쪽 책** → 자막마다 누가 지었는지 작은 표시 →
미션 1이 어린이집이라 **물감 · 스펀지** → 부모 리포트 *"어른과 같이 · 어린이집 · 8쪽"* · 말하기 **9번**(전 4번)

---

## v0.10 — 일기 모드 (오늘 있었던 일로 짓기)

**9/21**

**왜**: 사용자 요청 — *"일기모드_설계.md 파일을 보고 일기모드 만들어줘"*
근거 문서는 `일기모드_설계.md`(2026-09-18 안치영 · 9/21 레포 규격과 대조해 §0 · §3 · §5 · §7 고침).

**한 줄로**: **일기 모드는 일기를 만들지 않는다.** 아이에게 오늘 무슨 일이 있었는지 묻고,
그 **실제 사실을 재료로** 동화 모드와 똑같은 6~8쪽 동화책을 만든다 (설계 §0).
"일기"는 팀 안에서만 쓰는 이름이라 **화면 어디에도 쓰지 않았다.**

| 무엇 | 내용 |
| --- | --- |
| 들어가는 자리 | 첫 화면에 [🌙 오늘 있었던 일로 만들기] 한 개. 두 갈래로 갈라지는 **유일한 자리**다 (§1) |
| 새 화면 | **장면 3′ 하나뿐.** 동화 모드의 질문 자리(장면 3~10)를 이것이 대신한다 — 질문 세트와 칸 목록만 갈아끼웠다 |
| 그대로 쓴 것 | 함께할 사람 · 도감 · 러너 · 판정 · 수준 3단계 · 무응답 흐름 · 책 만들기 · 책 · 미션 2개 · 친구 평가 · 선물 · 책장 · 부모 모드 |
| 칸 | 기 `place` · 승 `problem` · 전 `cause`(S1 자리) · 결 `solution` — **새 칸 이름을 만들지 않았다**(슬롯 12종 안). 선택 칸 `reaction` · `newcomer` · `name` · `title` / 묻지 않는 칸 `sound` · `adult` · `companion` (§2) |
| 질문 | 턴마다 **사다리** 3~4칸. 말이 없으면 답을 고르게 하지 않고 **질문을 바꿔 다시 묻는다.** 아래로 갈수록 기억을 덜 꺼내고 눈앞의 것에 답하게 된다 (§4) |
| 끝나는 조건 | 동화 모드와 **같은 셋** — `story_ready` · `mascot_pick` 2회 연속 · 15분 경과. 새로 만들지 않았다 (§3) |
| 빈 칸 | 사다리가 다 떨어지면 마스코트가 채우고 `by: mascot`으로 남긴다. 책 자막에는 나오고 **주고받기 횟수 · 수준 신호 · 리포트 원문 인용에서는 빠진다** (§5 · §5-1) |
| 책 | 기 1 / (등장 1) / 승 1 / (기분 1) / 전 2 / 결 1 / 에필로그 1 = **6~8쪽.** 미션 2개는 뼈대 그대로, 소품만 하루에서 나온 것으로(가방에 묻은 흙) (§7-1) |
| 이벤트 | 더한 것은 `story_start`의 `mode: diary` **한 필드뿐.** 새 이벤트는 없다 (§8) |

### 설계가 못 박은 "하지 않는 것"을 코드로 지킨 자리

- **마스코트가 장소를 지어내지 않는다** — 사다리가 다 떨어져도 `place`는 *"오늘 있었던 곳"*으로만 채운다.
  데이터는 `by`가 지켜 주지만, 아이가 가지 않은 곳이 그 아이의 하루로 적히는 것은 **내용 문제**다 (§3-2)
- **사다리 뒤에 그림 카드 3장을 붙이지 않는다** — 일기에서 그림 3장은 앱이 아이 하루를 추측해 보여 주는 것이 된다 (§7-6, 후자로 기운 안)
- **[직접 그리기 🖍️]는 일기 모드에도 남겼다** — 없으면 부모 리포트의 '만들기' 축이 빈칸으로 나온다 (§8)
- **턴 상한을 두지 않았다** — 8턴 상한은 핵심 지표인 「주고받기 횟수」를 스스로 깎는다 (CLAUDE.md 규칙 ③)

### 손댄 파일

새로: `demo/DiaryBank.kt`(질문 세트 · 책 한 장) · `test/DiaryTest.kt` · `test/DiaryFlowTest.kt`
수정: `demo/Model.kt`(모드 · 출처 `by` · 필수 칸) · `demo/Scenes.kt`(장면 3′ · 갈림길) · `demo/Director.kt`(사다리) ·
`demo/StoryBank.kt`(제목 · 템플릿 확정 예외) · `demo/Missions.kt`(일기 소품) · `demo/Behind.kt` ·
`ui/Screen.kt`(첫 화면 버튼 · 서랍) · `ui/Book.kt`(탈것 · 공룡 빼기) · `ui/Parent.kt`(오늘의 기록) · `MainActivity.kt`(진행 막대)

### 확인

- `./gradlew :app:assembleDebug` 통과 · 새 경고 없음
- 단위 검사 **24개 전부 통과** (일기 17 · 기존 7). 기존 `StoryTextTest`(템플릿 5 × 함께할 사람 6 × 장소 4 전수 자막 검사)가 그대로 통과 → 동화 모드 회귀 없음
- `DiaryFlowTest`는 감독을 실제로 띄워 한 바퀴 돌린다: ① 다 말한 날 → `story_ready` · 네 칸 `by: child`
  ② 한마디도 안 한 날 → 질문이 4번 이상 바뀌고 카드는 안 뜨며 `mascot_pick` 2회로 끝 · 인용 0개
  ③ 동화 모드는 예전 그대로 시작 · `story_start`에 `mode=story`
- 만든 책 자막 전수는 `app/build/diary_samples.txt`에 남는다
- **Pixel_3a 에뮬레이터에서 한 바퀴 통과** — 시작 화면 [오늘 있었던 일로] → 함께할 사람 → 도감 → S3′ 네 질문 →
  책 만들기 → 표지 → 6쪽(미션 1 흙 털기 · 미션 2 딸기 건네기) → 선물 → 부모 모드.
  화면으로 확인한 것: 진행 막대가 네 칸으로 차는 것 · [직접 그리기]가 2턴부터 나오는 것 ·
  표지와 책에 로켓 · 공룡이 없는 것 · 상태 탭의 칸마다 `[by: child]` · 끝난 조건 `story_ready` ·
  「말한 방식」 원그래프에 마스코트 항목이 없는 것 · 기준 질문 비교가 "오늘 어디 갔었어?"로 바뀐 것

### 에뮬레이터에서 찾아 고친 것

**부모 리포트 질문 카드에 없는 친구가 나왔다.** 아이가 아무도 그리지 않은 날에도
*"새 친구는 내일은 뭐 하고 놀까?"* 가 떴다(`ui/Parent.kt`의 이름 없을 때 기본값 "새 친구").
동화 모드는 늘 친구가 있어 드러나지 않던 자리다. **없는 친구를 앱이 만들어 내면 안 되므로**(§3-2)
일기 모드에서는 이름이 있을 때만 그 카드를 넣고, 없으면 함께한 사람에게 묻는 질문으로 채운다.

### 아직 정하지 않은 것 (설계 §7 · §9 그대로 열어 둠)

§7-2 월 권수(매일 쓰는 기능인데 기본 요금제가 월 2권) · §7-3 수준을 모드별로 볼까 ·
§7-4 완전 무응답일 때(데모는 두 갈림길을 다 눌러 볼 수 있게 둠) · §7-5 15분이 취침 루틴에 긴가 ·
§9-3 판정 평가셋에 일기 문항 추가(지금 평가셋은 상상 세계 문항만이고 **기억 인출 문항과 순차 답 함정이 없다**)

---

## v0.9.1 — 팀 이름 스플래시 (CLAP)

**9/17 17:55 ~ 18:30**

**왜**: 사용자 요청 — *"앱 실행하면 스플래시 스크린 뜨게 해주는데 우리 팀명이 CLLAP이니까 CLLAP로 띄워줘"*
→ 이어서 *"아래에 말로짓는인형극이라는 텍스트는 지워주고 CLLAP만 나오게 해줘"* → *"CLAP으로 해줘 CLLAP말고"*

| 무엇 | 내용 |
| --- | --- |
| 화면 | 크림색 바탕에 **C L A P** 네 글자만 — 빨강 · 노랑 · 초록 · 파랑으로 하나씩 통통 튀어 오름 → 잠시 뒤 옅어지며 첫 화면 |
| 뺀 것 | 처음 넣었던 노란 밑줄 · "말로 짓는 인형극" 문구 (요청) |
| 시간 | 약 2.4초 · **누르면 바로 넘어감** |
| 만든 방법 | 첫 화면 위에 덮는 Compose 화면(`ui/Splash.kt`) — 뒤의 시작 흐름은 그대로 진행되므로 이야기 · 서랍 동작은 바뀌지 않음 |
| 켜는 순간 | 앱 창 배경 · Android 12+ 시스템 스플래시 배경을 같은 크림색(`splash_bg #FFF4E1`)으로 → 흰 화면이 번쩍이지 않음. 시스템 스플래시에는 앱 아이콘(마스코트)이 잠깐 보인 뒤 CLAP로 이어진다 (트러블슈팅 3-14) |

### 손댄 파일

새로: `ui/Splash.kt` · `res/values-v31/themes.xml`
수정: `MainActivity.kt` · `res/values/themes.xml` · `res/values/colors.xml`

### 확인

- 빌드 통과 · Pixel_3a에서 앱을 새로 켜서 연속 캡처 → 마스코트(시스템) → 빈 크림색 → **CLAP**(문구 · 밑줄 없음) → 첫 화면 순서 확인
- 에뮬레이터가 또 `offline`에서 안 돌아와 데이터를 지우고 새로 부팅함 (트러블슈팅 6-8) — 그 직후라 앱이 켜지는 데 몇 초 걸렸다

---

## v0.9 — 함께 하는 사람 · 질문 은행 · 수준별 템플릿 · 배경 속 것

**9/17 15:10 ~ 17:15**

**왜**: 사용자 요청 (요지)
- *"배경에 기존에 있는 그림들이 상호작용 되는 느낌인거지 상호작용되는 요소들을 추가하는게 아니야"* — 구름 배경에 구름을 또 얹으면 이상하다
- *"아이랑 같이 게임하는사람이 이모라고 특정 짓지말고 사용자한태 누구랑 같이 하냐고 물어보고"*
- 소크라틱 질문으로 발달 상태 · 인지 능력을 판단하는데 **질문이 너무 적다** → 더미 데이터로라도
- 동화책이 **발달 상태별로 알맞은 템플릿**으로 진행되는지 검토 · 내용이 **물 흐르듯** · **6~8장**
- **처음 고정 질문 뒤로는 매번 다른 질문 · 다양한 답** → 책이 매번 다르게
- 도중 추가: *"누구랑 하는지 그림카드를 띄워서 선택이 아니라 녹음으로 하는 형식으로 만들어줘."*

> ⚠️ **v0.8의 "배경 오브젝트"(미리 만든 그림 13장을 배경 위에 얹기)를 되돌렸다.**
> 지금은 배경 그림에 **이미 그려진 것**의 자리를 좌표로 정해 두고, 그 자리가 반짝이고 통 튄다. 오브젝트 그림 10장은 앱에서 뺐다.

### 1. 검토 — 요청 전(v0.8) 상태

| 요청 | v0.8 실제 | 판정 |
| --- | --- | --- |
| 수준별 템플릿 | 3턴째에 **"오해-화해형(B)" 문자열을 늘 같은 값으로** 넣음 · 책은 수준과 상관없이 늘 6쪽 같은 구성 | ❌ 템플릿이 수준을 따르지 않음 |
| 수준 판단 | 수준이 문자열 · S1 2턴 연속이면 "다음 세션 까닭 짓기"만 표시 · 판단 근거가 화면 어디에도 없음 | ❌ 판단 과정이 안 보임 |
| 질문 수 · 다양성 | 질문마다 고정 문장 1개 · 답 5개 · 매번 같은 질문 | ❌ |
| 이야기 흐름 | 6쪽 자막이 서로 독립 (3쪽 "쳐다보고 있었어요" 다음 4쪽 미션 문장이 갑자기) | △ |
| 쪽 수 | 늘 6쪽 | ❌ (6~8쪽이어야) |
| 함께 하는 사람 | 모든 대사 · 책 · 기록이 "이모"로 고정 | ❌ |
| 배경 상호작용 | 배경 위에 구름 · 별 그림을 새로 얹음 | ❌ (요청과 반대) |

### 2. 함께 하는 사람 (장면 1↳ 새로)

- 시작 → **"오늘은 누구랑 같이 이야기를 만들어? 마이크를 누르고 말해 줘!"** → 녹음 → 호칭 사전(엄마 · 아빠 · 이모 · 할머니 · 할아버지 · 친구)
- **그림 카드 없음** (추가 요청). 말하기 전에는 마스코트 ＋ "?" 빈 자리, 말하면 그 사람의 펠트 그림이 톡 나타남 (새 그림 6장)
- 무응답 → "옆에 누가 있어? 이름을 불러 줄래?" → "함께 온 사람한테 손을 흔들어 봐! 누구야?" → 그래도 없으면 "엄마"로 두고 진행
- 이후 **호칭 · 말투가 전부 그 사람을 따름**

| 곳 | 엄마 · 아빠 · 이모 | 할머니 · 할아버지 (높임) | 친구 |
| --- | --- | --- | --- |
| 도착 대사 | "우와, 우주? 로켓 타고 가는 거야?" | 같음 (어른이 아이에게 하는 말) | "우와, 우주? 나도 갈래!" |
| 참여 질문 | "엄마, 지호와 뿌뿌가 친해지게 엄마는 뭘 도와줄까?" · "엄마가 우주에 같이 간다면 뭘 챙겨 갈까?" | "할머니, … 뭘 도와주실래요?" · "할머니께서도 … 챙겨 가실래요?" | "친구야, 뿌뿌랑 친해지게 너는 뭘 해 줄래?" |
| 책 한 줄 | "엄마는 셋의 사진을 찍어 주었어요" | "할머니께서는 맛있는 떡을 싸 오셨어요" | "친구는 술래잡기를 하자고 했어요" |
| 도움 요청 쪽(C) | "엄마가 '말해 줘서 고마워' 하고 꼭 안아 주었어요" | "할머니께 달려가 … 할머니께서 '말해 줘서 고맙구나' 하고 꼭 안아 주셨어요" | "친구가 '내가 같이 있을게!' 하고 손을 잡아 주었어요" |
| 표지 · 부모 기록 | "함께 엄마" · "엄마와 함께" · 함께하기 축 "엄마와 n번 주고받았어요" | | |

### 3. 질문 은행 · 발달 판단 (`demo/StoryBank.kt` 새로)

- **고정 질문 2개**(어디로 가 볼까 · 누가 흔들었을까) 뒤로는 **질문 은행**에서 뽑는다 — 칸(slot) 19개 · **변형 46개 · 더미 답 258개** (고정 질문 2개는 따로 · 답 7~9개)
- 변형마다 **어느 수준에서 쓰는지**(고르며 · 이어 · 까닭)와 **무엇을 보려는 질문인지**(예: "'왜' 질문에 까닭을 담나 (S1)")가 붙어 있다
- **안 쓴 변형 → 가장 오래전에 쓴 변형** 순으로 뽑아 다음 이야기에서 같은 질문이 바로 나오지 않는다 · 여러 템플릿이 함께 쓰는 칸(해결)은 **지금 템플릿의 변형만**
- 더미 답마다 **수준 표시(①②③)와 신호(S1 · S2 요소 · A1)**. 서랍 **[더미 아이 답: 고르며 · 이어 · 까닭 짓기처럼]** 을 고르면 그 수준의 답이 70%로 나온다
- **판단 규칙** (역할1 조사2 §3~4)
  - 말로 답한 것만 센다 · 마음 말하기는 신호 아님
  - 올림: 고르며 → 이어는 S1 또는 S2 · **이어 → 까닭은 S1 + 구조(계기 · 시도 · 결과 또는 잇는 말)** — 2턴 연속이면 한 칸
  - 내림: 카드 · 무응답 · 두 어절 이하에 신호 없음이 2턴 연속이면 한 칸
  - **3턴째(까닭 질문 뒤) 템플릿 확정** · 그 뒤 이동은 질문 난이도와 다음 세션 시작점만 바꿈
  - 세션 누적 추정: S1 ≥ 2 · 요소 ≥ 2 → 까닭 / 짧은 답 · 카드 60% 이상 · 신호 없음 → 고르며 / 나머지 → 이어
- 서랍 **🧠 발달 판단** 탭 — 턴마다 질문 · 답 · 어절 수 · 신호, 누적 추정, 템플릿 확정 근거, 이번 이야기 질문 id
- 서랍 **[이번 이야기 시작 수준]** — 지난 세션 종료 단계를 흉내

### 4. 수준별 템플릿 · 6~8쪽 (역할1 조사2 표 #1 · 역할2)

| 수준 | 템플릿 | 쪽 | 이야기 잇기 질문 (장면 6↳ 새로) | 매듭 질문 |
| --- | --- | --- | --- | --- |
| 고르며 짓기 | **E 여정형** (되풀이 말) | 6 | 어디를 지나갈까 · 거기서 뭘 봤어 | 뭐 하고 놀까 · 어디에 도착할까 |
| 이어 짓기 | **C 위험-대응-도움형** | 7 | 뭐라고 말할까 · 누구에게 도움을 청할까 | 사이좋게 지내려면 |
| 이어 짓기 (길 잃음 · 배고픔 · 다침) | **D 방문자-직업수행형** | 7 | 뭐가 필요할까 · 어떤 사람이 되어 도울까 | 기운 나게 뭘 해 줄까 |
| 까닭 짓기 | **A 도전-성취형** | 8 | 처음엔 어떻게 해 봤을까 · 왜 잘 안 됐을까 | 다른 방법은 · 무엇을 알게 됐을까 |
| 까닭 짓기 (장난 · 힘자랑) | **G 우화-교훈형** | 8 | 계속 흔들다 어떻게 됐을까 · 무엇을 알게 됐을까 | 다시 친해지려면 · 무엇을 알게 됐을까 |

- 속성 축(결정 13): 심심 · 놀고 싶음 · 인사 → 상황 이해 / 장난 · 힘자랑 → 교훈 / D → 직업 체험 / E → 체험
- 쪽마다 **앞 쪽의 결과를 받아 다음 쪽을 여는 문장** — 예 (A): 까닭 → 첫 시도 → **실패와 그 까닭** → 미션 1("게다가…포기하지 않고") → 다시 생각 → 미션 2("이번엔 달랐어요!") → 해냈다 · 알게 된 것
- 책 쪽 구성도 쪽 종류대로 — 출발 · 흔들림(친구 등장) · 대화 · 여정(배경 속 것 모두 반짝) · 실패(풀 죽은 친구 · "…") · 미션 · 함께(함께 하는 사람 그림)
- 위 오른쪽 점 · "n/6 · n/7 · n/8" · 마지막 쪽에서 📚 · 표지에 "여정형 · 6쪽"
- 제목도 템플릿으로: 『보리와 떠난 인어 궁전 여행』 · 『"그만!" 용감한 지호』 · 『의사 선생님 지호와 뭉치』 · 『다시 해 본 지호와 뿌뿌』 · 『장난꾸러기 뭉치와 지호』

### 5. 배경 그림 속 것 (`ui/Hotspots.kt` 새로)

- 배경 4장마다 이미 그려진 것 6~9곳을 좌표로: 우주(달 · 행성 · 별) · 바닷속(거품 · 미역 · 산호 · 바위) · 공룡 나라(구름 · 화산 · 야자나무 · 바위 · 아기 공룡) · 눈밭(눈 덮인 나무 · 먼 나무 · 눈송이)
- 화면 크기에 맞춰 잘린 배경(Crop)과 같은 계산으로 자리 맞춤 · 겹쳐 그린 배경으로 좌표 확인
- **말한 것** → 둘레에 금빛 고리 + ✨ 가 숨 쉬듯 반짝 · 그 자리의 배경 조각이 통 튄다
- **누르면** 그 조각이 통 튀고 "빙그르르~" · "뽀글!" · "둥실~" (책에서 돋보기면 이름 · 깃털이면 "간질간질~")
- 사건 장면 · 흔들림 쪽에서는 배경 조각들이 같이 흔들림
- 무응답이면 모든 것을 한 번 반짝여 보여 줌 · 다음 장면으로는 [다음으로] 또는 5초

### 6. 검토하다 찾아 고친 것

| 증상 | 고침 |
| --- | --- |
| 주인공 고르기 "오늘은 **누구랑** 모험을 떠날까?" — 바로 앞 "누구랑 같이 만들어?"와 뜻이 겹침 | "오늘 이야기의 주인공은 누구로 할까?" |
| 함께 하는 사람 화면에 주인공 그림 (아직 고르기 전) | 마스코트로 |
| 고른 카드 뒤에 흰 네모 (카드 방식일 때) | 카드 방식 자체를 녹음으로 바꿈 |
| 반짝임이 옅어 캡처에 안 잡힘 | 금빛 고리 · ✨ · 밝기 0.45~1 |
| 표지 안내 "▶ 를 눌러 펼쳐 봐!"가 제목을 덮음 | 표지 내용을 아래로 |
| 이어 짓기 흉내인데 3턴째 까닭 짓기로 올라감 | 이어 → 까닭 올림 조건에 "구조" 추가 |
| C 템플릿인데 "도착하면 보리랑 뭐 하고 놀까?"(E 질문) | 함께 쓰는 칸은 템플릿 변형만 |
| "과자를 건넸어요. 하지만 **목소리가 너무 커서**…" | 실패 까닭을 어떤 시도에도 맞는 것으로 |
| "그러다 뭉치**도** 넘어졌어요" (다른 누구도 안 넘어짐) | "뭉치가" |
| 눈밭 이야기에 "쿵쿵 발자국 길 · 공룡 알 둥지" | 눈밭 전용 지나갈 곳 · 도착할 곳 · 본 것 |
| 눈밭 "눈 언덕" 고리가 빈 눈밭 위에 뜸 | 언덕 삭제 · 먼 나무 2곳 추가 · 눈송이 고리 축소 |
| "별이가 **둘이** 신나게 춤을 춰!" · "**노래**를 끌어서 건네줄래?" | "신나게 노래하며 춤을 춰!" · "음표를 끌어서" |
| 부모 기록 카드 세 장이 같은 문장을 인용 · "속상했, 행복했던 마음" | 카드마다 다른 말 · "속상했던, 행복했던 마음" |

### 손댄 파일

새로: `demo/StoryBank.kt` (질문 은행 · 판단 · 템플릿) · `ui/Hotspots.kt` · `test/StoryTextTest.kt`
전면 수정: `demo/Model.kt` · `demo/Director.kt` · `demo/Scenes.kt` · `ui/Book.kt`
수정: `demo/Behind.kt` · `demo/Missions.kt` · `ui/Screen.kt`(함께 하는 사람 · 🧠 발달 판단 탭 · 더미 아이 수준) · `ui/Parent.kt`
그림: 함께 하는 사람 6장 (`ic_p_*` 512px) · 오브젝트 10장 뺌 (`obj_rock · obj_bubble · obj_star`는 미션 소품이라 남김) → drawable **122장**

### 확인

- 빌드 · APK **34.6MB** · 코드 19파일 약 7,100줄 + 테스트 1파일 · 공유 zip **26.0MB** (190파일 · 그림 122 · 문서 4)
- 장면 이동으로 공룡 나라 → "정말 구름이 있네!" → 그려진 구름 · 화산을 눌러 봄: 금빛 고리 · 그 조각이 통 튀고 "부글부글!"
- **글자 검사 테스트 5개 통과** — 템플릿 5 × 사람 6 × 장소 4 × 답 조합(약 2,000줄)에서 자리표시 · 겹친 조사 · 높임말 누락 없음 · 템플릿 6~8쪽 · 수준 → 템플릿 표 · 변형마다 답 4개 이상 · 수준 섞임 · 연달아 같은 질문 안 나옴 · 함께 쓰는 칸은 템플릿 변형만
- Pixel_3a 가로로 **4회**
  1. 무작위 · 이모 · 바닷속 → 짧은 답으로 고르며 짓기 → **E 6쪽** 『보리와 떠난 인어 궁전 여행』 · 미션 2개 · 이모 그림 · 한 줄
  2. 까닭 짓기 · 아빠 · 우주 → 까닭 짓기 전용 질문("우주에 사는 친구들은…" · "지호가 운석이었다면…") → **A 8쪽** 『다시 해 본 지호와 뿌뿌』 · 친구 평가 · 선물 · 책장 · 부모 기록 "아빠와 함께 · 8쪽"
  3. 이어 짓기 · 이모 · 눈 오는 데 → **C 7쪽** (2쪽 "…붙잡고 마구 흔들고", 5쪽 "이모에게 달려가…")
  4. 이어 짓기 · 할머니 · 눈 오는 데(수정 후) → **C 7쪽** · "할머니께서도 … 챙겨 가실래요?" · "좋아요! 할머니도 함께예요!" · 마지막 쪽 "할머니께서는 맛있는 떡을 싸 오셨어요" · 나무 고리가 그려진 나무 위에
- 질문 변형은 회차마다 달라짐: 장면 3 "뭐가 있을까"는 "창밖을 봐! …" · "…사는 친구들은 뭘 보면서 놀까?" 등, 장면 4 이어 묻기는 "누가 제일 깜짝 놀랐을까?" · "그때 지호는 뭐라고 말했을까?" · 장면 7 공룡 질문 3종
  단, **3 · 4회는 앱을 새로 켠 뒤라 같은 이어 묻기가 나온 적이 있음** — "지난 이야기와 다른 질문"은 같은 실행 안에서만 보장하며, 이는 단위 테스트로 확인
- 남은 한계: D · G 템플릿은 기기에서 끝까지 돌리지 않고 글자 테스트로만 확인 · 앱을 끄면 "지난 이야기에서 쓴 질문" 기억이 사라짐(데모는 저장소 없음)

---

## v0.8 — 화면 전체 쓰기 · 소크라틱 질문 · 오브젝트 · 대화로 만든 미션 · 책장 · 부모 설정

**9/17 오후 ~ 15:05**

**왜**: 사용자 요청 — 화면마다 이름 표시 · 아래 마스코트 띠를 없애 화면을 넓게 · 진행 표시를 막대 + 별로 ·
배경마다 상호작용 오브젝트를 미리 만들어 즉시 배치 · 모든 질문을 공룡 장면처럼 소크라틱으로 · 장면별 수정 16건 ·
마지막에 책장까지. 이후 추가 요청 *"로딩바가 제일 최상단으로 가게 해야돼"*.

### 1. 화면 전체

| 무엇 | 전 | 후 |
| --- | --- | --- |
| 화면 이름 | 없음 | **모든 장면 맨 위 가운데** ("캐릭터 생성하기" · "어디로 갈지 정하기" …) · 부모 모드는 화면 안 고정 머리 |
| 마스코트 | 아래 116dp 띠에 늘 고정 | 띠를 없애고 **아래 왼쪽에 떠 있는 말풍선** · 말할 때 톡 튀어나오고 한 자씩 써짐 · 마스코트가 콩콩 |
| 🎤 ➡️ | 띠 안에 늘 | **아래 오른쪽에 떠 있음 · 쓸 수 있을 때만** |
| 진행 표시 | 별 6개 | **맨 위(화면 이름보다 위) 진행 막대 + 끝에 별** — 칸마다 로딩처럼 차오르고, 다 차면 막대 · 별이 금빛으로 반짝 → 동화책 |
| 버튼을 눌렀을 때 | 네모난 물결(리플) | 그림 · 책 버튼은 **물결 없음** |

### 2. 질문 — 소크라틱 (공룡 장면 방식)

| 곳 | 전 | 후 |
| --- | --- | --- |
| 주인공 만들기 | "머리는 짧아, 길어, 묶었어?" | "우리 주인공은 머리가 어떻게 생겼을까?" · 무응답 "거울 속 지호 머리를 떠올려 봐" |
| 장면 3 | + [직접 그리기] | **그리기 삭제** · 이어서 **"우주에는 뭐가 있을 것 같아?"** |
| 장면 4 | 무응답이면 카드 · "그래서 어떻게 됐어?" | **"누가 흔들었을까?" 카드 없음** · 무응답 "창문 밖을 잘 봐" → "쉿, 뭔가 움직였어!" → "혹시 ○○일까?" · **"그다음엔 어떻게 됐을 것 같아?"** |
| 장면 5 | 까닭 한 번 | 까닭 짓기 조건일 때만 **"지호가 ○○였다면 기분이 어땠을까?"** (역할1 조사2 #5) |
| 장면 6 | "이름은 뭐야?" (이모) | **"○○한테 어울리는 이름은 뭘까?"** · 무응답 "어떤 소리가 떠올라?" |
| 장면 7 | "티라노, 긴목공룡, 뿔공룡이 있어. 어떤 공룡이야?" | **"공룡? 어떤 공룡이야? 어떻게 생겼어?"** |
| 장면 9 | "어떤 게 마음에 안 들어?" | "어디를 바꾸면 더 좋을까?" |
| 장면 10 | 해결만 | + **"그러면 뿌뿌 기분은 어떨까?"** |
| 답 후보 | extra(S2)만 | **reason(S1) 표시 추가** — 까닭 · 방법을 담은 답 |

### 3. 오브젝트 (장면 3~ 모든 배경)

- 장소마다 4~5개를 **미리 생성**(ComfyUI 13장) · 자리도 미리 정함 (하늘 · 땅 가장자리 — 주인공 · 친구 · 공룡 · 자막 · 버튼 자리를 피함)
- "○○에는 뭐가 있을 것 같아?"에 답하면 **말한 것부터 톡 튀어나오고 조금 크게** → 나머지도 올라옴 · 생성 대기 0초
- 이후 모든 장면 · 책 6쪽에 그대로 · 누르면 흔들리며 "반짝!" · "쿵!" · 장면 4에서는 같이 흔들림

### 4. 장면별 수정

| 장면 | 요청 | 한 것 |
| --- | --- | --- |
| 1 | 마스코트 숨기기 · 부모/아이 버튼 · 별 재화 표시 | 아래 말풍선 없음 · **[🧒 아이 \| 👪 부모]** · **별 아이콘 + 왼쪽 변 없는 둥근 틀 + 숫자** (한도 끄면 ∞) |
| 3 | 그리기 삭제 · 말풍선 애니메이션 | 삭제 · 말풍선 튀어나옴 + 타자 효과 (전 장면) |
| 4 | 카드 삭제 · "누가 흔들었을까?" | 위 2번 |
| 10 | 이모 질문 오류 · 혼자 선택 | **"이모, 지호와 뿌뿌가 친해지게 이모는 뭘 도와줄까?"** · 답 5개 · **답이 없으면 대신 고르지 않고 책에도 넣지 않음** |
| 12(6쪽) | 이모 내용이 또 나오는 것 확인 | 원인 = 3초 뒤 마스코트가 대신 골라 넣은 것 → 위에서 고침. 이모가 답했을 때만 한 줄 |
| 11 | 제목은 지어 주기 · 책장에서 수정 | 친구 · 장소 · 해결 방법으로 제목 · 차오르는 막대 · "책 이름은 책장에서 바꿀 수 있어요" |
| 12 | 전체 화면 · 반응 글자 · 네모 박스 확인 · 대화 기반 미션 | 마스코트 · 아래 요소 없음 · **누른 것 바로 위에 글자** · 네모 박스 = 기본 리플(오류 아님) → 없앰 · **미션 1 = 누가 흔들었나(9종) · 미션 2 = 해결 방법(6종)** · 친구에게 놓으면 하트 |
| 14 | 대사 변경 · 이모 멘트 삭제 | "책 다 만들었다! 고생했어~~" · "우와, 엄마 오면 같이 보자" 삭제 |
| 부모 | 마스코트 · 요소 삭제 · UI 품질 · 한도 토글 · 상한 삭제 · 그림체 4종 · 시작 비밀번호 | 왼쪽 메뉴 + 카드형 · 6축 카드 · 도넛 · **Switch 토글 + 권수 [−][＋]** · 다시 그리기 상한 삭제 · **그림체 견본 4장**(결정안건 부록 6) · **시작 비밀번호 토글** |
| 끝 | 첫 페이지로 · 책장에 꽂히기까지 | **장면 15 책장** — 새 책이 내려와 꽂힘 · 새 책! · ✏️ 자리 · [부모 모드] [🏠 처음으로] · 다시 읽기는 넣지 않음 |

### 5. 검토하다 찾아 고친 것

| 증상 | 고침 |
| --- | --- |
| 진행 막대 왼쪽에 반투명 네모가 떠 있음 | 빛이 지나가는 효과가 막대 밖에 그려짐 → 막대 안에서만 |
| 진행 막대가 그림판 위를 덮음 | 무대 위 여백 58 → 80dp |
| "로켓**에 엔진에** 불이 붙었어요" | 흔적 문장에서 "엔진에" 뺌 |
| "심심했어**대**" | 전하는 말 도우미 `reported()` → "심심했대" |
| 미션 1 · 2의 친구 · 공룡이 안내문 · ▶와 겹침 | 자리 재배치 |
| 6쪽에서 우주 바람이 공룡과 겹침 · 나무가 ◀ 버튼을 가림 | 오브젝트 자리 전면 재배치 |
| 부모 모드 스크롤 내용 위로 "부모 모드" 이름이 떠 있음 | 화면 안 고정 머리로 옮김 |
| 부모 기록 "9번 말했어요" · 카드 "10번" | 둘 다 마이크 횟수로 |
| 프리셋을 골랐는데 "지호가 **그린** 상어" | "지호가 **고른** 상어" · 이모 멘트도 바꿈 |
| 처음으로 누르면 부모 기록이 비어 버림 | 이야기는 **새 이야기를 시작할 때** 지움 |
| 책장 책이 선반에서 떠 있음 | 선반 높이 맞춤 · 책 크기 조정 |

### 손댄 파일

새로: `demo/Missions.kt` · `ui/Chrome.kt` · `ui/Shelf.kt` · `ui/Parent.kt`
전면 수정: `demo/Model.kt` · `demo/Director.kt` · `demo/Scenes.kt` · `demo/Behind.kt` · `ui/Screen.kt` · `ui/Book.kt` · `MainActivity.kt`
그림: `tools/gen_extra.py` +26장 (오브젝트 13 · 미션 소품 8 · 그림체 견본 4 · 책장 배경) → drawable **126장**
문서: 시나리오(전면) · 설계 · 변경이력 · 트러블슈팅

### 확인

- clean 빌드 · APK **34.2MB** · 공유 zip **24.7MB** (그림 126 · 문서 4)
- Pixel_3a 가로로 **처음부터 책장 · 부모 모드까지 3회** (우주 · 바닷속 · 우주+프리셋 친구) + 장면 점프로 책 4~6쪽 · 부모 설정 따로
- 시작 비밀번호 켬 → 🔒 → 어른 확인 → 시작 · 별 3 → 2 · 처음으로 뒤에도 책장 3권 · 부모 기록 유지
- 장면 4 ➡️ 3번 → 관찰 → 힌트 → "혹시 문어일까?" (카드 안 뜸)
- 이모 무응답 → 6쪽 · 표지에 이모 없음 / 이모 답함 → "이모는 짝짝짝 박수를 쳐 주었어요" · "함께 이모"
- 미션: 외계인 → 불 · 물대포 / 딸기 → 딸기 건네기 · 하트 · 별 → 별 건네기
- 책에서 주인공을 누르면 바로 위에 "히히!" · 네모 물결 없음

---

## v0.7 — 구현대본 반영 · 열린 질문 · 친구 평가 · 어른 칸

**9/16 16:12 ~ 17:25**

**왜**: 조장이 `구현대본.md`(엔드픽처 초안 + 결정 30건 + 이 데모 시나리오를 합친 구현용 문서)를 냈다.
사용자 요청 — *"구현대본.md파일을 보고 데모버전을 수정해주고 … 오류나 잘못된 부분은 없나 확인하고 있으면 고쳐줘"*.
문서를 장면마다 코드와 대조하고, 에뮬레이터에서 처음부터 부모 모드까지 두 번(눈 오는 데 · 우주) 돌려 고쳤다.

> ⚠️ **9/15에 "엔드픽처 초안대로 장소 카드를 처음부터 띄운다"고 정했던 것을 구현대본 결정 30이 뒤집었다.**
> 지금은 모든 질문이 열린 질문이고, 카드는 무응답 · 모호한 말 확인 때만 나온다.

### 1. 구현대본과 달랐던 것 → 고침

| 곳 | 전 | 후 (구현대본 근거) |
| --- | --- | --- |
| 장면 3 장소 | 카드 3장이 질문과 함께 처음부터 · "공룡 나라, 바닷속, 우주가 있어…" | **열린 질문** "오늘은 어디로 가 볼까?" · 무응답이면 쉬운 질문 → 그때 카드 (§0-1 · 결정 30) |
| [직접 그리기] | 그리기형 아이에게만 카드 옆에 | **질문 내내 오른쪽 아래에 늘** · 무응답 사다리 중에도 · 누르면 그림판 → 칸이 참 (`mode=draw`) (§0-1) |
| 무응답 카드 교체 | 2번 | **최대 3번** 뒤 마스코트 (§5 · ⭐22) |
| 필수 칸 6개 | 장소 · 문제 · 까닭 · 등장인물 · **친구(공룡)** · 소리 | 장소 · 문제 · 까닭 · 등장인물 · 소리 · **해결** (§2) — 별 6개가 해결 뒤에 다 찬다 |
| 시작 화면 (S1) | [같이 시작하기] 하나 | **[이야기 만들기] · [📚 책장] · 하루 별 ⭐3 → ⭐2** · ⭐0이면 한 번만 안내 · "어른과 함께 하는 놀이예요" |
| 도감 (S2) | 주인공 + ＋ 한 칸 | **가로 4칸** · 빈 칸은 모두 **점선** ＋ |
| 주인공 수정 2회 뒤 | 이대로/골라서 → 골라서 만들기 | **만든 3장 중 고르기** (결정 29) · 확인 화면에 "다시 그리기 n/2" |
| 장면 4 사건 | 모든 장소에서 "○○가 흔들려" | **장소별** 로켓이 덜컹덜컹 · 물살이 소용돌이 · 땅이 쿵쿵 — 질문과 책 2쪽 (§6) |
| 장면 9 중간 확인 | 싫어 → 색 · 크기 · 표정 카드가 바로 | **"어떤 게 마음에 안 들어?" 열린 질문**(후보 5: 색 · 크기 · 표정 · 모양 · 종류) · 화면에 **[직접 그리기]** 카드 · 다시 생성 2회 다 쓰면 3장 중 고르기 (S7) |
| 느린 생성 | 로그 한 줄뿐 | 서랍 **[🐢 그림 생성 느리게]** → 8초 "그림은 조금 뒤에 올 거야" · 15초 취소 · 프리셋 확정 (S7 · §7) |
| 어른만 말함 | 1.4초 뒤 되묻기 | **2.5초** 기다렸다가 (§0-2) |

### 2. 구현대본에 있는데 없던 것 → 새로 만듦

| 무엇 | 내용 |
| --- | --- |
| **어른 전용 칸** (§2 · S3 해결) | 해결 뒤 "이모는 별을 따면 뭐 하고 싶어?" · 소원 카드 3장(펠트 그림) · 3초 무응답이면 마스코트가 고름 · **책 6쪽에 "이모는 ○○ 싶대요."** |
| **장면 13 친구 평가** (S10) | 오늘 만난 친구(아이 그림 · 공룡)마다 [또 만날래 💛] [안녕 👋] · 지우는 선택지 없음 · 장면이 15 → **16개** |
| **프리셋에 없는 장소** (§6) | "눈 오는 데!" → 🖌️ 배경 생성 → **새 눈밭 배경** · 탈것 · 사건 · 미션은 가까운 유형 · 대사 · 제목 · 책에 아이가 말한 장소 그대로 |
| **8턴 상한** (§2) | 이야기 턴이 8을 넘으면 남은 칸을 마스코트가 채우고 책으로 · 서랍 [⏩ 8턴 지난 것으로] |
| **이벤트 기록** (§0-5) | `story_start` · `utterance(speaker·mode)` · `slot_filled(source=voice/card/draw/mascot)` · `signal` · `emotion_mention` · `make` · `image_request` · `redraw` · `mission` · `friend_rating` · `session_end` → 서랍 **🧾 기록** 탭 |
| **부모 리포트 6축** (S12) | 말하기 · 이유 말하기 · 마음 말하기 · 이야기 채우기 · 만들기 · 함께하기 — 3단계 표시 + 원문 · **말한 방식 원그래프** · 본인 대비 · 누리과정 · 질문 카드 3장 · 고지 3줄 |
| 시연 서랍 | [그림 생성 느리게] · [하루 별 0으로] · [8턴 지난 것으로] · 🧾 기록 탭 · **아이 반응 버튼을 장면 목록 위로** (스크롤 줄임) · 체크리스트 18 → 21 |

### 3. 돌려 보다 찾은 버그 → 고침

| 증상 | 원인 | 고침 |
| --- | --- | --- |
| 제목이 『지호의 **공룡 나라** 여행』 (아이는 "눈 오는 데"라고 함) | 제목 후보가 프리셋 이름(`th.label`)을 씀 | 아이가 말한 장소(`placeName`) |
| 책 6쪽 "지호와 몽몽이는 **티라노와 티라노** 등에 타고…" | 해결 문장에 이미 공룡 이름이 있는데 앞에 또 붙임 | 앞의 공룡 이름을 뺌 |
| "이름은 뭐야?" 동안 그림판 · [다 그렸어]가 그대로 | 그린 뒤 화면을 바꾸지 않음 | 그린 그림만 보여 주는 `Stage.Show` |
| "어떤 게 마음에 안 들어?" 동안 좋아/싫어 카드가 남음 | 같은 이유 | 공룡만 보여 준다 |
| 친구 평가 카드의 [안녕 👋]이 잘림 | 버튼 두 개를 가로로 놓음 | 세로로 쌓음 |
| 그림판이 별 6개 줄에 가림 → 고친 뒤엔 [지우기]가 잘림 | 세로 여백 · 크레용 크기 | 위 여백 40dp · 크레용 36dp |
| 중간 확인 카드 3장째가 화면 밖 | 가로 740dp에 그림 + 카드 3장 | 카드 폭 158dp · 그림 170dp |
| [책장에 꽂기]가 선물 카드를 덮음 | 같은 Box 아래쪽에 겹침 | 카드 줄에 아래 여백 |
| 서랍의 [8턴 지난 것으로] 칩이 안 보임 | 칩 3개가 한 줄을 넘침 | 두 줄로 |
| 마스코트가 대신 고른 칸이 기록에 `source=card` | 탭과 구분하지 않음 | `Reply.Tapped(byMascot)` → `source=mascot` |

### 4. 새 그림 (ComfyUI · `tools/gen_extra.py`)

`bg_snow`(1344×768 눈밭) · `prop_necklace` · `prop_lamp` · `prop_pocket`(512px, 어른 칸 소원 카드) → drawable **96 → 100장**

### 손댄 파일

`demo/Model.kt`(Scene.FRIENDS · Stage.Show/FriendRate · Theme.eventAsk/eventLine · 필수 칸 · 하루 별 · 이벤트 · 말한 방식) ·
`demo/Director.kt`(열린 질문 · 직접 그리기 · 교체 3번 · event() · sourceOf()) ·
`demo/Scenes.kt`(S1 · 장소 · 사건 · 중간 확인 · 어른 칸 · 친구 평가 · 8턴 · 책 자막) · `demo/Behind.kt` ·
`ui/Screen.kt`(시작 화면 · 도감 점선 · 친구 평가 · 리포트 6축 · 서랍) · `ui/Book.kt`(배경 이름) · `MainActivity.kt`([직접 그리기]) ·
`tools/gen_extra.py`(새로) · 문서 4종

### 확인

- 빌드 성공 · APK **29.5MB** (그림 4장 추가)
- Pixel_3a 가로로 **처음부터 부모 모드까지 2회**: ① "눈 오는 데" → 새 배경 · 공룡 나라 뼈대 ② "로켓 타고 우주 갈래!" → 미션 2개 · 6쪽 자막 · 친구 평가
- 무응답 사다리: ➡️ → 쉬운 질문(카드 없음) → ➡️ → 카드 → 교체 1 · 2 · 3회째 → 마스코트 — 📊 상태 로그로 확인
- 중간 확인: 싫어 → 열린 질문 → "빨간 게 좋아!" → 색만 바뀜 · [직접 그리기] → 그림판 → 돌아옴(다시 그리기 0/2 그대로)
- 🧾 기록 탭: `story_start star_before=3 star_after=2` · `slot_filled … source=…` 쌓이는 것

---

## v0.6 — 용량 정리와 문서화

**9/16 14:50 ~ 15:50**

**왜**: 팀에 배포해야 하는데 프로젝트가 532MB였다. 그림이 전부 1024×1024라 91MB를 차지했다.

### 바뀐 것

| 무엇 | 전 | 후 |
| --- | --- | --- |
| `res/drawable` 96장 | 90.4MB (전부 1024px) | **17.9MB** — 화면에 실제로 보이는 크기로 리사이즈 |
| APK (clean build) | — | **28.2MB** |
| 소스 폴더 (빌드 산출물 제외) | 94MB | **21MB** |

리사이즈 기준 — 가로 2220px 기기에서 실제로 그려지는 크기의 약 2배:
배경 1344px · 주인공/공룡 640px · 새 친구/탈것/소품/선물 512px · **아이콘 256px**

### 만든 문서

| 문서 | 무엇 |
| --- | --- |
| `안드로이드데모_설계.md` | 여는 법 · **파일 공유 방법** · 구조 · 설계 원리 4가지 · 역할별로 볼 파일 · 그림 생성 방법 · 5분 시연 대본 |
| `안드로이드데모_시나리오.md` | 장면 14개 대본 · 답 후보 전체 · 갈래(무응답 · 어른만 말함 · 성향 3종) · 장소 3곳 비교표 |
| `안드로이드데모_변경이력.md` | 이 문서 |
| `안드로이드데모_트러블슈팅.md` | 막혔던 것 31개 (7개 절 · 날짜별 목록 포함) |

**배포 zip**: `C:\Android\finalproject_demo_공유.zip` (19.0MB) — 프로젝트 + `docs/` 폴더 안에 문서 4개.
`build/` `app/build/` `.gradle/` `.idea/` `local.properties` 제외(빼지 않으면 532MB).

### 확인

에뮬레이터에 설치해 공룡 나라 세계 화면 확인 — 리사이즈 전후 눈으로 차이 없음.
zip 안에 제외 대상이 0개인 것, drawable 96장이 들어간 것 확인.

---

## v0.5 — 아이콘을 전부 펠트 그림으로 · 장소별 미션

**9/16 14:30 ~ 14:50**

**왜**: 사용자 지적 — *"캐릭터 커스텀하는거 조금 기능이 이상하거든… 동화책에 나오는 인터렉티브 요소들도
이상하거든 이모티콘이나 아이콘들도 다시 comfyui로 제대로 만들어주고 불끄고 물 주고 하는것도
상황이나 배경 캐릭터요소에 맞게 바꿔줘."*

### 1. 이모지 → ComfyUI 펠트 그림 (42장 추가)

화면에 남아 있던 이모지를 전부 같은 그림체로 교체했다.

| 종류 | 무엇 |
| --- | --- |
| 아이콘 30장 | 마이크 · 다이얼 · 도구 4종(손 · 뿅망치 · 깃털 · 돋보기) · 좋아/싫어 · 색/크기/표정 · 기분 3종 · 해결 3종 · 머리/옷/안경 옵션 9종 · 스피커 · 책 · 크레용 · 화살표 |
| 미션 소품 12장 | 물대포 · 스펀지 · 진흙 · 조개 · 진주 · 연못 · 용암 · 연기 · 물튀김 · 우산 · 반짝 · 해초 |

`Art.Img(이름, 대신 그릴 것)` 구조라, 그림이 없으면 이모지로 대신 나온다 → 그림을 다 안 뽑아도 앱이 돈다.

### 2. 미션이 장소에 맞게 갈아 끼워짐

`MissionSpec`을 새로 만들어 장소마다 붙였다. 문구는 `{f}` 친구 · `{v}` 탈것 · `{c}` 아이 · `{cause}` 까닭으로 채운다.

| | 우주 | 바닷속 | 공룡 나라 |
| --- | --- | --- | --- |
| 미션 1 | 로켓 엔진의 **불** ← 물대포 | 거북이 등의 **진흙** ← 스펀지 | 기차의 **용암** ← 물대포 |
| 없어진 뒤 | 연기 | 거품 | 연기 |
| 미션 2 | 비구름 → 별 우물(물이 차오름) | 진주 → 조개(빛이 남) | 비구름 → 연못(물이 차오름) |

- 붙는 자리도 탈것에 맞췄다: 로켓은 **아래 엔진**, 거북이는 **등**, 기차는 **지붕**
- **도구가 손가락을 따라온다.** 문지르면 물방울이 튀고, 3개가 줄어들다 사라지면 탈것이 떠오르며 반짝
- 미션 2는 `fill = "water"`(수위가 차오름) / `"glow"`(목표 안에서 빛남) 두 연출

### 3. 주인공 커스텀 고침

| 문제 | 고침 |
| --- | --- |
| 3번째 질문 "공룡·자동차·별(좋아하는 것)"에 답해도 그림이 안 바뀜 — 옷 프린트가 전부 공룡이라 ⚠️ | 질문을 **"안경"**(동글 · 네모 · 안 씀)으로 교체. "뭘 바꿀까"의 답 후보도 머리 · 옷 · 안경으로 |
| 답하는 동안 빈 실루엣만 보임 | 답할 때마다 **그 속성이 붙은 인형이 미리보기**("이렇게 되고 있어") |
| 새 주인공 카드 이름이 길어 두 줄 | 안경이 있으면 옷 대신 안경으로 부름 — "짧은 머리 안경 지호" |

⚠️ **초안과 달라진 곳이라 회의 확인 필요.** 원안대로 가려면 옷 프린트 3종 그림이 더 필요하다.

### 4. 배치 정리

책에서 별 6개 숨김(이미 다 찼고 미션 힌트와 겹침) · 6쪽 반짝이를 가운데로 · 결말 선물이 어두워진 뒤 겹치던 것 수정.

### 손댄 파일

`demo/Model.kt`(MissionSpec · Theme) · `demo/Scenes.kt`(미션 대사 · 커스텀 질문) · `ui/Book.kt`(미션 2종 전면) ·
`ui/Screen.kt`(아이콘 교체) · `tools/gen_icons.py`(새로 만듦) · `res/drawable` +42장

### 확인

우주 · 바닷속 두 경로를 끝까지, 무응답 경로(➡️ → 다시 읽기 → 교체 → 마스코트 선택 / 쉬운 질문 → 카드)까지
에뮬레이터로 직접 돌림. 미션 1·2 모두 실제 손동작으로 성공.

---

## v0.4 — 동화책 품질 · 커스텀 UI 통일 · 답 무작위

**9/16 11:50 ~ 14:30**

**왜**: 사용자 지적 — *"동화책의 퀄리티가 html데모버전 보다 떨어져… 지호 캐릭터를 새로 생성할때
커스텀 부분도 기존에 만들어져있는 캐릭터처럼 같은 ui로… 지호의 답변도 똑같은 답변이 아니라
더미 데이터를 넣어서 질문당 5개정도 랜덤으로."*

### 1. 답이 질문마다 5개, 무작위

`Answer(글, 뜻하는 값, extra)` 구조를 만들었다. **답마다 뜻이 붙어 있어** 무작위로 골라도 이야기가 그 답대로 이어진다.

```kotlin
Answer("운석이 날아와서 쿵 했어!", "운석", extra = true)   // → 새 친구 = 운석, S2 신호
```

적용한 질문: 주인공 3종 · 수정 · 장소 · 왜 · 되묻기 · 까닭 · 이름 · 공룡 · 소리(공룡별) · 해결 · 제목 (15개 × 5)
시연 서랍에서 특정 답을 골라 시연할 수도 있다.

### 2. 책이 그림책처럼

| 전 | 후 |
| --- | --- |
| 흰 종이에 그림 몇 개 · 자막 한 줄 | **세계 배경 위에** 확정 그림을 얹은 6쪽 · 종이 테두리 · 그림자 |
| 다음 쪽만 | **◀ ▶ 양방향** · 쪽 점 표시 · 자막 띠 + 🔊 낭독 |
| 도구가 장식 | 도구 4종으로 **캐릭터를 누르면 반응**("간지러워!" · 돋보기는 설명) |
| 쪽마다 정지 | 1쪽 뒤뚱 · 2쪽 흔들림 + "쿵!" · 3쪽 친구가 스르르 등장 · 6쪽 반짝 |

### 3. 주인공 27장 + 눈 스티커

골라서·말로 만든 주인공이 도감 프리셋과 다른 그림체로 보이던 문제를 없앴다.

- ComfyUI로 **머리 3 × 옷 3 × 안경 3 = 27장**을 같은 펠트 그림체로 생성
- 눈(반달 · 별)은 **스티커 오버레이** — 기획안 §4의 "표정은 눈 스티커" 방식
- 눈 위치는 `tools/find_eyes.py`가 그림에서 자동으로 찾아 `ui/HeroEyes.kt`에 씀 (27장 + 프리셋 1장 = 28개)

### 4. 그 밖

- 아이 그림이 가로로 그리면 세로로 찌그러지던 것 → 비율 저장 + **그린 부분만 잘라** 크게
- 장면 8에 [🔁 다시 (2번 남음)] [👍 이 소리로] 화면 버튼 (전에는 서랍에만 있었음)
- 결말에 [📚 책장에 꽂기] [👪 부모 모드] 화면 버튼
- 부모 기록이 **오늘 실제 대화**로 채워짐 (인용문 · S1 횟수 · 그린 친구 이름 · 마음 말하기)

### 손댄 파일

`demo/Model.kt` · `demo/Director.kt`(Answer) · `demo/Scenes.kt`(전면) · `ui/Book.kt`(새로 만듦) ·
`ui/HeroImage.kt` · `ui/HeroEyes.kt`(새로 만듦) · `tools/gen_heroes.py` `tools/find_eyes.py`(새로 만듦)

---

## v0.3 — ComfyUI 그림 · 글꼴 · 앱 아이콘

**9/16 10:30 ~ 11:50**

**왜**: 사용자 요청 — *"html에 만든거 그대로 안따라해도되고… 상용화된 앱들 처럼 만들어줘.
comfyui사용해도되니까 디자인좀 이쁘게 뽑아봐."*

### 바뀐 것

- **그림 24장 생성**: 무대 배경 · 세계 배경 3장 · 마스코트 · 주인공 프리셋 2 · 공룡 3 · 탈것 3 · 새 친구 9 · 선물 2
  (우리 PC ComfyUI `krea2_turbo_nvfp4`, 펠트 3D 그림체 → BiRefNet으로 배경 제거)
- **Jua 글꼴**(OFL) 적용 — 웹 데모와 같은 둥근 한글
- **앱 아이콘**을 마스코트로 (어댑티브 아이콘 · 노란 배경)
- 시작 화면을 무대 커튼 배경 + 유리 패널 + 큰 CTA로
- 카드에 그림자 · 고르면 살짝 커짐 · 마이크를 FAB 스타일로
- 공룡 색 바꾸기를 **색상 회전**으로 (⭐26: 재생성 없이)
- `ui/Assets.kt` — 그림을 이름으로 찾고 **없으면 벡터로 대신 그리는** 구조

### 손댄 파일

`ui/Assets.kt` `ui/Type.kt`(새로 만듦) · `ui/Screen.kt` · `res/drawable` +24장 · `res/font/jua.ttf` ·
`res/mipmap-*`(아이콘) · `tools/gen_assets.py`(새로 만듦)

---

## v0.2 — 문서 교차검증 · 마이크 온/오프 · 아이가 고른 것이 책이 된다

**9/16 10:00 ~ 10:30**

**왜**: 사용자 지시 — *"정렬회의_결정안건.md랑 엔드픽처_초안.md보면서 기능들 다시 검증해주고
녹음 버튼은 시간 제한이 있는게 아니라 온/오프 형식으로… 정해진 그림카드로 가게 하지말고
내가 직접 선택한걸로 만화책이 만들어지게."*

### 1. 마이크 온/오프

| 전 | 후 |
| --- | --- |
| 질문이 끝나면 대기 시간(5·8·7초)이 자동으로 흐르고 시간이 다 되면 무응답 | 🎤를 누르면 듣고 **다시 누르면 끝. 시간 제한 없음** |
| — | ➡️ = "말 없이 넘김"(무응답) |
| — | 실제 앱 규칙(⭐5)은 서랍의 **"무응답 타이머 켜기"** 로 볼 수 있게 남김 |

### 2. 아이가 고른 것이 이야기를 바꾼다

장소를 `Theme`로 만들어 **3곳**(우주 · 바닷속 · 공룡 나라)을 넣고, 고른 장소가 배경 · 탈것 ·
새 친구 후보 · 책 자막 · 제목까지 정하게 했다. 새 친구 · 공룡 · 까닭 · 해결 방법 · 이름 · 제목도 전부 칸에 저장해 책에 반영.

### 3. 문서 교차검증으로 고친 것

| 장면 | 어긋났던 것 | 고침 |
| --- | --- | --- |
| 4 | '왜' 답 뒤 되묻기 없음 · S2를 잘못 셈 | "그래서 어떻게 됐어?" 추가. 물어본 결과는 S2 아님, 마음 말하기는 부모 기록 재료로만 |
| 4 무응답 | 카드가 외계인·바람·별똥별 | 초안대로 외계인·운석·바람 (장소별로 다름) |
| 5 | 이모 대사 · 순서 | 이모가 먼저 말함 → "○○는 어떻게 생각해?" 한 번 (⭐5) |
| 6 | 프리셋 3장 안 보임 | "뿌뿌·반짝·동글 ○○이 있어. 누가 좋아?" |
| 7 | 공룡 이름 | 티라노 · 긴목공룡 · 뿔공룡 · "공룡도 데려갈래!"를 아이가 먼저 |
| 8 | [다시] 없음 | 🔁 다시(2번) · "와, 멋진 소리야!" · 한 번 더 → 기본 효과음 |
| 11 | 제목을 안 물음 | "이 책 이름은 뭐라고 할까?" 마이크로 |
| 12 | 3쪽뿐, 도구가 다름 | 6쪽 · 손·뿅망치·깃털·돋보기 · 장난 반응 · 6쪽 공룡 누르면 녹음 소리 |
| 13 | 선물 동시에 | 차례로 하나씩 → 책장 → 어두워짐 |
| 14 | 기록이 고정 문구 | 오늘 실제 대화로 채움 |

---

## v0.1 — 안드로이드 첫 이식

**9/16 09:34 ~ 10:00**

**왜**: 19번 결정(Kotlin 네이티브)에 맞춰, 실제로 만들 때 쓸 구조를 미리 잡으려고.
사용자가 만든 빈 프로젝트(`C:\Android\finalproject_demo`, "No Activity" Views 템플릿)에 Compose를 얹었다.

### 바뀐 것

- Views 템플릿 → **Compose**(BOM 2024.09.00 · kotlin-compose 플러그인 · `buildFeatures.compose`)
- **가로 고정**(`sensorLandscape`) · 전체 화면
- 장면 15개를 **중단 가능한 함수(suspend fun)** 로 이식 — HTML의 콜백 사슬 대신 위에서 아래로 읽히는 코드
- 엔진(`demo/`)과 화면(`ui/`)을 분리 — 진짜 STT·LLM을 붙일 때 `Director.ask()` 한 곳만 바꾸면 되게
- 웹 데모의 SVG를 **작은 경로 파서**로 이식(`ui/Art.kt`) — 그림이 없어도 앱이 돌게
- 그림판(Canvas + 드래그) · 미션 2종(문지르기 · 끌기)
- **시연 서랍** — 화면 오른쪽 위 구석 길게 누르기 (아이 화면을 깨끗하게)

### 만든 파일

`demo/Model.kt` `demo/Director.kt` `demo/Scenes.kt` `demo/Behind.kt` ·
`ui/Art.kt` `ui/Theme.kt` `ui/Screen.kt` · `MainActivity.kt`

---

## v0.0 — HTML 데모 교차검증

**9/15 저녁 ~ 9/16 오전**

안드로이드 이식 전, 웹 데모(`prototype/엔드픽처_데모.html`)를 문서와 대조해 고친 것.

| 어긋났던 것 | 고침 |
| --- | --- |
| 질문과 그림 카드가 늘 같이 떴다 | **열린 질문**은 마이크만, 말이 없을 때만 카드 (기획안 화면 4·5) |
| 선택형 질문의 카드 시점이 문서끼리 충돌 | **초안대로 처음부터**로 확정(9/15). 탭한 답은 수준 신호 아님, 고르기형 첫 턴 탭은 무응답 아님(⭐6) |
| 주인공 만들기에 프리셋 토글이 없었다 | [말로 만들기] / [골라서 만들기] 두 방식 (기획안 화면 2 · 81조합) |
| 장면 6 이름이 자동 입력 | 마이크 질문으로. 무응답이면 "외계인 친구" |
| 장면 9에서 색 바꾸기가 재생성 + 횟수 소모 | 색·크기·표정은 **생성 없이 즉시**, 종류만 재생성 2회 (⭐26 · 24) |
| "처음부터"가 새로고침이라 서버가 없으면 실패 | 페이지 안에서 상태만 되돌림 |
| 조사 오류("하늘가 말함") | 받침 판별 조사(이/가 · 은/는 · 와/과) |

---

## 지금 상태 (v0.8 · 9/17 15:05 기준)

| 항목 | 값 |
| --- | --- |
| 기준 문서 | **`구현대본.md`** (9/16 조장) — 엔드픽처 초안 · 결정안건보다 우선 |
| Kotlin 소스 | 17개 파일 · 약 5,800줄 |
| 그림 | `res/drawable` 126장 |
| 생성 스크립트 | `tools/` 5개 (gen_assets · gen_heroes · gen_icons · gen_extra · find_eyes) |
| APK | 34.2MB (debug · clean) |
| 장면 | 17개 (장면 1~16 + 주인공 만들기) · 책장 · 비밀번호 화면 |
| 장소 | 프리셋 3곳 · 각각 미션 2종 + 프리셋에 없는 장소 1곳(눈 오는 데 · 배경 생성) |
| 답 후보 | 질문 19개 × 5개 (어른 질문 포함) |
| 확인 방법 | 에뮬레이터(Pixel_3a 가로)로 장면 1 → 부모 모드까지 직접 |

---

## 앞으로 기록하는 법

데모를 고칠 때마다 **같은 작업 안에서** 이 문서를 갱신한다(끝났다고 보고하기 전에).

1. 맨 위 표에 새 줄(**날짜 · 시각 포함**), 본문에 새 절을 **맨 앞에** 추가하고 제목 아래에 **`**M/D HH:MM**`** 한 줄
   날짜가 바뀌면 "하루 타임라인"을 그 날짜용으로 새로 만든다
2. 각 절에 반드시 넣을 것 — **왜**(누가 무엇을 요청했나 · 무엇이 문제였나) · **바뀐 것**(전/후 표) ·
   **손댄 파일** · **확인**(어떻게 눈으로 확인했나)
3. 막힌 문제가 있었으면 `안드로이드데모_트러블슈팅.md`에도 추가 — 제목 아래 **``M/D · v0.x``** 한 줄,
   증상 → 원인 → 해결, 그리고 그 문서 맨 위 "날짜별 목록"에도 한 줄
4. 배포 zip을 다시 만든다 (`안드로이드데모_설계.md` §2의 파워셸 명령)
