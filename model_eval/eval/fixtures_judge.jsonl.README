안치영에게 받은 100개 평가셋을 `eval/fixtures_judge.jsonl` 이름으로 이 위치에 넣으세요.

현재 기대 형식의 핵심 키:
- id
- slots
- asked
- template
- utterance

오전에는 gold가 없어도 됩니다. 먼저 실제 모델 예측을 수집할 수 있습니다.
오후 박진웅의 gold가 합쳐지면 같은 raw JSONL을 `python score_existing.py`로 재채점합니다(API 재호출 0건).

최신 예시는 `fixtures_judge_demo.jsonl`을 참고하세요.
