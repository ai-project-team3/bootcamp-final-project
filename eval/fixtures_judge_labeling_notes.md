# `fixtures_judge.jsonl` 라벨 기준 — 적용한 대로 고쳐 둔 것

> `guidelines/2_공통_데이터_모델.md` §2-2 기준표를 100개에 실제 적용하면서 애매했던 경우와 어떻게 정했는지 적는다. 안치영 검수 20개와 대조 전이라 **일치도는 아직 비어 있다.**

## 기준표 요약

| 필드 | true 조건 | false 예 |
|---|---|---|
| `slot_1`(slot_match) | 지금 묻는 칸이 아니어도, 뜻이 통하는 답이면 그 칸을 채운다 | 무관한 말, 되묻기 |
| `contradiction` | 이미 채워진 칸과 사실이 어긋날 때만 | 앞 내용 없으면 무조건 false |
| `s1_reason` | **"왜"에 답했을 때** — `~니까`·`~때문에`·`왜냐하면`·인과의 `~서`(등 같은 기능의 어미) | 순차의 `~서`, 단순 서술, 조건("그러면")·결과 접속사("그래서")로 이어진 서사 |
| `s2_addition` | 묻지 않은 것을 스스로 더 말함(절이 둘 이상이면 대개 true) | 한 절짜리 단답 |

## 경계 사례 — 어떻게 정했나

| 사례 | 애매했던 점 | 어떻게 정했나 |
|---|---|---|
| j001, j013, j029 (`무관` 유형) | 안치영 라벨은 "무관"인데, 내용상 이미 채워진 칸(`place`·`problem`·`newcomer`)과 사실이 정면으로 어긋남 | **`무관`(안 채워짐)과 `contradiction`은 별개 축**이라 판단 — slot_1은 null로 두되 contradiction은 내용 기준으로 따로 true 처리 |
| "그러면"(j034, j057) | `~서`는 아니지만 결과·조건을 설명해서 인과처럼 읽힘 | 기준표가 요구하는 "왜에 답했다"는 **행동의 까닭**이지 결과/조건이 아니라고 보고 `s1_reason: false` — 마커가 명시된 넷(`~니까`·`~때문에`·`왜냐하면`·`~서`) 밖은 기본적으로 false, 단 `~거든`(j058)처럼 기능이 같은 어미는 true로 인정 |
| "그래서"(j009·j010·j038·j061·j084·j092) | 접속사 "그래서"가 인과처럼 보임 | **서사를 잇는 결과 접속사**(A했다, 그래서 B했다)와 **자기 행동의 까닭을 댄 것**은 다르다고 보고 false로 통일 |
| j100 (유형 라벨은 `S1`인데 내용은 순차 함정) | "엄마한테 달려가**서** 말해"의 `~서`가 인과처럼 보이지만 실제로는 "달려간 다음 말한다"는 순서 | 유형 라벨을 그대로 믿지 않고 **문장 자체를 다시 판정** — `s1_reason: false` (숨은 함정으로 남겨둠, 안치영·최민우에 공유 필요) |
| `미완결` 유형 15개 중 12개 | 원래 설계는 "미완결 → 대개 null"인데, 실제로는 "달님 마을!"처럼 뜻이 분명한 한 단어 답이 대부분이었음 | meta에 `'주의': '음성 인식이 뭉갠 말'`이 **명시된 3개만**(j006·j040·j055) null 처리, 나머지 12개는 "문법이 틀려도 뜻이 통하면 true" 원칙대로 정상 채움 — 이 원칙이 "미완결"이라는 유형 이름보다 우선한다고 판단 |
| `story_ready` | 100개가 전부 한 턴짜리라 세션 전체 완결을 볼 수 없음 | 이 턴 **직전**에 필수 6칸(`place·problem·cause·newcomer·sound·solution`) 중 이 턴에 묻는 칸 하나만 비어 있었고, 이 턴이 그걸 채우거나(`no_longer_needed`로) 해제하면 `true` — 그 외엔 전부 `false`로 보수적으로 처리 |
| `next_slot_ok` | "맥락상 여러 칸이 될 수 있다"를 100개에 일관되게 적용할 기준이 필요 | place→newcomer→problem→cause→reaction→name→sound→solution→adult→title **고정 순서**에서, 이 턴 반영 후 안 채워진 칸 중 **앞쪽 최대 3개**를 허용 집합으로 — `story_ready:true`면 빈 배열 |

## 아직 못한 것

- **안치영 검수 20개와 대조 → 일치도 숫자로 기록**: 검수용 20개가 아직 없어서 못함. 안치영이 20개를 별도로 라벨하면 위 표의 경계 사례부터 먼저 맞춰보면 될 것 같다(특히 `무관`+`contradiction` 동시 처리, `S1` 라벨 내부의 숨은 순차 함정).
