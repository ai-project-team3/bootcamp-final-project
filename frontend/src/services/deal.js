// Owner: 역할 5. One file per router. See guidelines/3_API_명세.md.
import client from "../api/client.js";

export const BASE = "/deal";

// TODO(역할 5): implement the calls for this router.
export default { BASE, client };
